---
phase: 05-telegram-linking-notifications
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - supabase/functions/telegram-bot-webhook/index.ts
  - supabase/functions/_shared/telegram.ts
  - features/resident/ResidentSettingsPage.tsx
  - app/app/settings/page.tsx
  - lib/validation.ts
autonomous: false
requirements: [TLGM-03, TLGM-04]
must_haves:
  truths:
    - "Resident can use approved Telegram commands/deep links to see only their own billing status and history summary."
    - "Admin-like users can receive scoped operational alerts/summaries relevant to payment verification."
    - "Resident can see Telegram link state in settings and safely relink/unlink from app context."
  artifacts:
    - path: "supabase/functions/telegram-bot-webhook/index.ts"
      provides: "Webhook command handler with profile-scoped authorization"
    - path: "features/resident/ResidentSettingsPage.tsx"
      provides: "Telegram link status + relink/unlink UI"
    - path: "lib/validation.ts"
      provides: "Command payload and link-action validation"
  key_links:
    - from: "telegram webhook command"
      to: "resident invoice query"
      via: "profile-mapped kavling scope"
      pattern: "auth.uid\(|kavling_residents|invoices"
    - from: "settings UI"
      to: "link/unlink functions"
      via: "explicit resident action"
      pattern: "functions\.invoke\(\"link-telegram-account\"|functions\.invoke\(\"telegram-bot-webhook\""
---

<objective>
Deliver resident/admin Telegram utility flows (commands + settings visibility) on top of secure linking and dispatch foundations.

Purpose: make Telegram useful for daily operations while keeping app/Supabase as system of record.
Output: command webhook, scoped billing shortcuts, admin alert path, and settings link-state UX.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@features/resident/ResidentSettingsPage.tsx
@app/app/settings/page.tsx
@supabase/functions/create-payment-submission/index.ts
@supabase/functions/get-proof-signed-url/index.ts

<interfaces>
From features/payments/submissionNotificationPlaceholder.ts:
```typescript
export interface SubmissionNotificationPlaceholderInput {
  submissionId: string;
  invoiceId: string;
  outcome: "approved" | "rejected";
}
```

From supabase/migrations/0002_tables.sql (relevant columns):
```sql
public.telegram_accounts(profile_id, telegram_user_id, telegram_chat_id, allows_notifications)
public.telegram_link_tokens(profile_id, token_hash, expires_at, consumed_at)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement Telegram webhook commands with strict profile scope</name>
  <files>supabase/functions/telegram-bot-webhook/index.ts, supabase/functions/_shared/telegram.ts, lib/validation.ts</files>
  <behavior>
    - Test 1: `/status` returns only linked resident billing summary and never other resident records.
    - Test 2: unsupported command returns safe help response.
    - Test 3: admin-like alert command returns operational summary payload only for allowed roles.
  </behavior>
  <action>Create webhook handler for approved commands/deep-link payloads and map Telegram user to linked profile before any query. Enforce profile-scoped reads for resident commands and role-gated reads for admin-like summary commands. Keep responses in concise Indonesian copy.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Command handler serves resident/admin utility flows without cross-account data exposure.</done>
</task>

<task type="auto">
  <name>task 2: expose Telegram link status and relink/unlink controls in resident settings</name>
  <files>features/resident/ResidentSettingsPage.tsx, app/app/settings/page.tsx</files>
  <action>Extend settings page to show current Telegram link state (linked account metadata + last seen), generate relink action, and support unlink confirmation flow. Preserve existing profile/preference behavior and validation constraints.</action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <done>Resident can manage Telegram linkage from `/app/settings` without affecting protected identity fields.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: human verify end-to-end Telegram flows</name>
  <files>MISSING — checkpoint verification only</files>
  <action>Validate the integrated resident/admin Telegram experience after automation tasks complete.</action>
  <verify>
    <automated>MISSING — Wave 2 uses human verification checkpoint after automated checks in tasks 1-2.</automated>
  </verify>
  <done>Human confirms resident/admin flows pass the checklist or reports exact failing step.</done>
  <what-built>End-to-end Telegram command + settings management experience</what-built>
  <how-to-verify>
    1. Open `/app/settings` as resident and confirm Telegram status card shows linked/unlinked state.
    2. Trigger link flow using generated token and confirm status updates in-app.
    3. Send approved resident command (e.g., `/status`) and confirm only own billing summary appears.
    4. Verify admin-like command output only from admin-linked account.
    5. Confirm unsupported command returns safe help text without data leakage.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list failed steps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram webhook → command handler | Untrusted command text and sender identity |
| Settings UI → link/unlink APIs | Resident-triggered account binding changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | S | webhook sender identity | mitigate | Resolve sender through linked `telegram_user_id` and reject unknown senders. |
| T-05-14 | T | command parameters | mitigate | Validate command schema and reject malformed args before DB access. |
| T-05-15 | R | command execution audit | mitigate | Persist command execution metadata and errors in audit trail. |
| T-05-16 | I | cross-resident data leak | mitigate | Apply profile-bound query filters and role checks before response rendering. |
| T-05-17 | D | command spam | accept | Accept basic rate limiting in v1; monitor and tune after launch. |
| T-05-18 | E | admin summary escalation | mitigate | Require admin-like role assertion from profile before privileged command output. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run test:unit`
- Human checklist in checkpoint task
</verification>

<success_criteria>
- Resident commands/deep links expose only self-scoped billing information.
- Admin-like Telegram summary path is role-gated.
- Resident settings clearly surfaces Telegram link state and management actions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-telegram-linking-notifications/05-03-SUMMARY.md`
</output>
