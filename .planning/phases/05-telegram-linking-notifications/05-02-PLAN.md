---
phase: 05-telegram-linking-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/send-telegram-notification/index.ts
  - supabase/functions/_shared/telegram.ts
  - supabase/functions/_shared/notifications.ts
  - supabase/migrations/0014_m08_notification_dispatch.sql
  - supabase/tests/sql/m08_notification_dispatch.sql
  - features/payments/submissionNotificationPlaceholder.ts
autonomous: true
requirements: [TLGM-02, COMM-05]
user_setup:
  - service: telegram
    why: "Send production notifications through neighborhood bot"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather -> HTTP API token"
must_haves:
  truths:
    - "Resident receives Telegram notifications only when linked and telegram preference is enabled for that category."
    - "Payment review outcomes and urgent/new announcement events enqueue and dispatch Telegram notifications."
    - "Delivery attempts are recorded for audit and troubleshooting."
  artifacts:
    - path: "supabase/functions/send-telegram-notification/index.ts"
      provides: "Telegram send endpoint with template rendering and API call"
    - path: "supabase/migrations/0014_m08_notification_dispatch.sql"
      provides: "Dispatch helper RPC and delivery status persistence"
    - path: "features/payments/submissionNotificationPlaceholder.ts"
      provides: "Real dispatch trigger for payment review outcomes"
  key_links:
    - from: "payment review + announcement workflows"
      to: "send-telegram-notification"
      via: "function invoke"
      pattern: "functions\.invoke\(\"send-telegram-notification\""
    - from: "notification preferences"
      to: "dispatch eligibility"
      via: "category + telegram_enabled filter"
      pattern: "notification_preferences|telegram_enabled"
---

<objective>
Implement Telegram notification dispatch so billing and communication events are delivered reliably with preference-aware filtering.

Purpose: satisfy resident notification outcomes without turning Telegram into source-of-truth business state.
Output: dispatch function, persistence/audit trail, and real event trigger wiring.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/0002_tables.sql
@supabase/migrations/0007_seed_initial_data.sql
@features/payments/submissionNotificationPlaceholder.ts
@features/resident/ResidentSettingsPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement notification dispatch DB contract and tests</name>
  <files>supabase/migrations/0014_m08_notification_dispatch.sql, supabase/tests/sql/m08_notification_dispatch.sql, package.json</files>
  <behavior>
    - Test 1: linked resident with `telegram_enabled=true` for category is eligible for dispatch.
    - Test 2: unlinked resident or disabled preference is excluded.
    - Test 3: dispatch attempt persists success/failure status with timestamp.
  </behavior>
  <action>Create SQL helpers/RPC for notification eligibility + delivery logging using existing `notification_preferences`, `telegram_accounts`, and notification history tables. Add SQL tests and wire into `test:sql` script. Keep category mapping aligned with existing preference categories.</action>
  <verify>
    <automated>npm run test:sql</automated>
  </verify>
  <done>Dispatch contract is tested and enforces link+preference gating.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: build Telegram sender function with template rendering</name>
  <files>supabase/functions/send-telegram-notification/index.ts, supabase/functions/_shared/telegram.ts, supabase/functions/_shared/notifications.ts</files>
  <behavior>
    - Test 1: template + variables render expected message body.
    - Test 2: Telegram API non-200 response records failed status and returns typed error.
    - Test 3: successful send records message id and delivered status.
  </behavior>
  <action>Implement a dedicated sender Edge Function that loads template data, renders placeholders, calls Telegram `sendMessage`, and persists attempt outcome. Reuse shared helper modules for API call and payload normalization. Do not leak bot token to browser-side code.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Function can send template-based Telegram messages with auditable status persistence.</done>
</task>

<task type="auto">
  <name>task 3: replace payment notification placeholder with real dispatch trigger</name>
  <files>features/payments/submissionNotificationPlaceholder.ts</files>
  <action>Replace the no-op placeholder by invoking `send-telegram-notification` for `approved`/`rejected` outcomes and correct category mapping (`payment_status`). Ensure failures do not corrupt payment verification transaction state; log/report non-blocking notification errors.</action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <done>Payment review flow now emits real Telegram notifications while preserving payment-state consistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal app event → send function | Event payload could be malformed or overbroad |
| send function → Telegram HTTP API | Third-party API call and response are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | S | recipient targeting | mitigate | Resolve recipients from DB link + preference data only; never trust caller-supplied chat id. |
| T-05-08 | T | template variable injection | mitigate | Whitelist template variables and sanitize rendered payload. |
| T-05-09 | R | notification accountability | mitigate | Persist each attempt with status/error metadata for auditability. |
| T-05-10 | I | secret exposure | mitigate | Keep TELEGRAM_BOT_TOKEN in function env only; no browser usage. |
| T-05-11 | D | repeated failed retries | accept | Accept bounded retry in v1; monitor via delivery status table. |
| T-05-12 | E | bypassing preference checks | mitigate | Enforce eligibility in SQL contract before send attempt. |
</threat_model>

<verification>
- `npm run test:sql`
- `npm run typecheck`
- `npm run test:unit`
</verification>

<success_criteria>
- Telegram sends are preference-aware, linked-account-only, and auditable.
- Payment review notifications are no longer placeholder no-op behavior.
- Notification dispatch failures are visible and non-destructive to core billing/payment state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-telegram-linking-notifications/05-02-SUMMARY.md`
</output>
