---
phase: 06-imports-optional-qris-launch-readiness
plan: 04
type: execute
wave: 2
depends_on: [06-03]
files_modified:
  - features/payments/QrisPaymentPanel.tsx
  - features/billing/InvoiceDetailPage.tsx
  - features/payments/PaymentSubmissionForm.tsx
  - app/admin/settings/page.tsx
  - features/settings/PaymentGatewaySettingsCard.tsx
  - lib/__tests__/qrisFeatureFlagFlow.test.ts
autonomous: true
requirements: [QRIS-01, QRIS-03]
must_haves:
  truths:
    - "Admin can keep gateway flag disabled and manual transfer still works fully."
    - "When enabled, resident sees QRIS action only for eligible invoices and can initiate payment."
  artifacts:
    - path: "features/settings/PaymentGatewaySettingsCard.tsx"
      provides: "Admin toggle for QRIS feature flag"
    - path: "features/payments/QrisPaymentPanel.tsx"
      provides: "Resident QRIS initiation UI"
    - path: "lib/__tests__/qrisFeatureFlagFlow.test.ts"
      provides: "Regression coverage for enabled/disabled behavior"
  key_links:
    - from: "PaymentGatewaySettingsCard.tsx"
      to: "app_settings"
      via: "Supabase upsert"
      pattern: "from\(\"app_settings\"\)"
    - from: "InvoiceDetailPage.tsx"
      to: "QrisPaymentPanel"
      via: "conditional render by feature flag + invoice eligibility"
      pattern: "qris|gateway"
---

<objective>
Deliver feature-flagged QRIS UX while preserving manual-transfer launch safety.

Purpose: Satisfy optional QRIS requirements without introducing launch-time regression risk.
Output: Admin gateway toggle + resident QRIS panel + fallback regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@features/billing/InvoiceDetailPage.tsx
@features/payments/PaymentSubmissionForm.tsx
@app/admin/settings/page.tsx

<interfaces>
From features/payments/PaymentSubmissionForm.tsx:
```typescript
interface PaymentSubmissionFormProps {
  invoiceId: string;
  invoiceStatus: string;
  outstandingAmount: number;
  onSubmitted: () => Promise<void> | void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement admin QRIS feature-flag controls</name>
  <files>features/settings/PaymentGatewaySettingsCard.tsx, app/admin/settings/page.tsx</files>
  <behavior>
    - Admin can toggle `payment_gateway_enabled` on/off.
    - Non-admin roles cannot access this control.
    - Toggle state persists via `app_settings`.
  </behavior>
  <action>Create a dedicated settings card (Indonesian copy) for QRIS feature management, reading/writing `app_settings` key `payment_gateway`. Include explicit warning that manual transfer is launch baseline and must remain operable when disabled.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>Admin settings persist QRIS on/off state with clear launch-safety messaging.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add resident QRIS panel and disabled-mode fallback regression test</name>
  <files>features/payments/QrisPaymentPanel.tsx, features/billing/InvoiceDetailPage.tsx, features/payments/PaymentSubmissionForm.tsx, lib/__tests__/qrisFeatureFlagFlow.test.ts</files>
  <behavior>
    - Disabled flag: only manual transfer submission path is shown and functional.
    - Enabled flag: eligible invoice shows QRIS panel that invokes create-qris function.
    - Ineligible statuses never show QRIS action.
  </behavior>
  <action>Integrate a QRIS panel into invoice detail alongside existing manual transfer card. Gate panel by flag + invoice eligibility; keep existing manual flow unchanged. Add unit contract test validating both disabled and enabled branches plus manual transfer continuity (QRIS-03).</action>
  <verify><automated>npm run test:unit -- qrisFeatureFlagFlow.test.ts</automated></verify>
  <done>Resident payment UI supports optional QRIS without regressing manual-transfer workflow when disabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-browser→app_settings | Privileged feature toggle write path |
| resident-browser→create-qris-function | User payment initiation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | E | gateway settings UI | mitigate | Enforce admin/super_admin-only route access and server-side RLS policy on `app_settings`. |
| T-06-10 | T | invoice payment UI branching | mitigate | Keep manual transfer component unchanged; add explicit tests for disabled flag fallback path. |
</threat_model>

<verification>
- `npm run test:unit -- qrisFeatureFlagFlow.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- QRIS can be toggled by admin without affecting manual transfer behavior when off.
- Eligible residents can initiate QRIS only when feature flag is enabled.
</success_criteria>

<output>
After completion, create `.planning/phases/06-imports-optional-qris-launch-readiness/06-04-SUMMARY.md`
</output>
