---
phase: 06-imports-optional-qris-launch-readiness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/imports/importTypes.ts
  - lib/imports/importPreview.ts
  - lib/__tests__/importPreview.test.ts
  - lib/validation.ts
autonomous: true
requirements: [IMPT-01, IMPT-02, IMPT-03]
must_haves:
  truths:
    - "Admin can upload CSV candidate data and receive row-level validation feedback before any write is applied."
    - "Validation works for kavling, resident-mapping, and fee-override imports with explicit error reasons."
  artifacts:
    - path: "lib/imports/importTypes.ts"
      provides: "Typed contracts for three import CSV shapes and preview errors"
    - path: "lib/imports/importPreview.ts"
      provides: "Pure preview parser/validator with no database writes"
    - path: "lib/__tests__/importPreview.test.ts"
      provides: "Deterministic validation contract tests for all import types"
  key_links:
    - from: "lib/imports/importPreview.ts"
      to: "lib/validation.ts"
      via: "zod schema composition"
      pattern: "safeParse|z\."
---

<objective>
Create a test-driven CSV preview validation core for all phase-6 import types.

Purpose: Establish a single, reliable import contract before UI/Edge-function wiring.
Output: Typed import contracts + pure preview engine + passing tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/validation.ts

<interfaces>
From lib/validation.ts:
```typescript
export const uuidSchema = z.uuid();
export const requiredStringSchema = z.string().trim().min(1, "Wajib diisi");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: write failing import preview contract tests (RED)</name>
  <files>lib/__tests__/importPreview.test.ts</files>
  <action>Create failing tests covering IMPT-01/02/03: valid kavling rows, valid resident+kavling mapping rows, valid fee override rows, and invalid rows (missing required fields, duplicate keys, malformed numeric/date values). Include assertions that preview output contains total rows, valid rows, invalid rows, and per-row Indonesian error messages.</action>
  <verify><automated>npm run test:unit -- importPreview.test.ts</automated></verify>
  <done>Test file exists and fails for missing implementation with clear expected output contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement typed preview parser and validators (GREEN)</name>
  <files>lib/imports/importTypes.ts, lib/imports/importPreview.ts, lib/validation.ts</files>
  <behavior>
    - Valid CSV row input returns normalized typed rows with zero errors.
    - Invalid row input returns indexed error entries without throwing.
    - Preview is read-only and does not invoke Supabase/network I/O.
  </behavior>
  <action>Add import-specific schemas and implement a pure preview function (`buildImportPreview`) that accepts parsed CSV rows plus import type and returns deterministic `{rowCount, validCount, invalidCount, errors, previewRows}`. Reuse zod patterns from `lib/validation.ts`; avoid any database writes in this module.</action>
  <verify><automated>npm run test:unit -- importPreview.test.ts</automated></verify>
  <done>All preview contract tests pass with deterministic output for all three import types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-browser→preview-parser | Untrusted CSV content enters validation pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `lib/imports/importPreview.ts` | mitigate | Strict zod validation + row index tracking, reject malformed/overflow values before persistence. |
| T-06-02 | D | CSV preview processing | mitigate | Bound maximum preview rows and fail fast with explicit error when input exceeds configured limit. |
</threat_model>

<verification>
- `npm run test:unit -- importPreview.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Preview engine returns accurate counts and row-level errors for kavling, resident mapping, and fee override CSV datasets.
- No persistence side effects occur during preview.
</success_criteria>

<output>
After completion, create `.planning/phases/06-imports-optional-qris-launch-readiness/06-01-SUMMARY.md`
</output>
