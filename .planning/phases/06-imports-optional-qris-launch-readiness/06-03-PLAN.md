---
phase: 06-imports-optional-qris-launch-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0013_m08_qris_gateway.sql
  - supabase/functions/create-qris-transaction/index.ts
  - supabase/functions/midtrans-webhook/index.ts
  - supabase/functions/_shared/midtrans.ts
  - supabase/tests/sql/m08_qris_reconciliation.sql
  - package.json
autonomous: true
requirements: [QRIS-01, QRIS-02]
user_setup:
  - service: midtrans
    why: "QRIS payment initiation and notification reconciliation"
    env_vars:
      - name: MIDTRANS_SERVER_KEY
        source: "Midtrans Dashboard -> Settings -> Access Keys"
      - name: MIDTRANS_IS_PRODUCTION
        source: "Deployment environment configuration"
    dashboard_config:
      - task: "Set payment notification URL to Supabase function endpoint"
        location: "Midtrans Dashboard -> Settings -> Configuration"
must_haves:
  truths:
    - "When QRIS is enabled, resident can initiate QRIS payment for eligible unpaid invoice."
    - "Gateway notifications update the correct invoice/payment state once, without duplication."
  artifacts:
    - path: "supabase/functions/create-qris-transaction/index.ts"
      provides: "Initiates QRIS transaction and stores provider metadata"
    - path: "supabase/functions/midtrans-webhook/index.ts"
      provides: "Validates and reconciles Midtrans notifications"
    - path: "supabase/tests/sql/m08_qris_reconciliation.sql"
      provides: "Idempotent reconciliation coverage"
  key_links:
    - from: "create-qris-transaction"
      to: "payment_gateway_transactions"
      via: "insert pending transaction"
      pattern: "from\(\"payment_gateway_transactions\"\).*insert"
    - from: "midtrans-webhook"
      to: "invoices/payments"
      via: "status reconciliation logic"
      pattern: "settlement|expire|pending"
---

<objective>
Implement backend QRIS initiation and notification reconciliation with Midtrans.

Purpose: Provide optional gateway path while keeping payment-state consistency trustworthy.
Output: Migration + QRIS create/webhook functions + SQL reconciliation tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/0002_tables.sql
@supabase/functions/create-payment-submission/index.ts

<interfaces>
From Midtrans docs research:
```text
notification fields: order_id, transaction_status, status_code, gross_amount, signature_key
QRIS statuses: pending, settlement, expire, deny
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add QRIS reconciliation SQL contract (RED) and migration support (GREEN)</name>
  <files>supabase/tests/sql/m08_qris_reconciliation.sql, supabase/migrations/0013_m08_qris_gateway.sql, package.json</files>
  <behavior>
    - Settlement marks gateway transaction and invoice/payment state exactly once.
    - Duplicate webhook payload does not duplicate payments.
    - Expire/deny statuses do not incorrectly mark invoice as paid.
  </behavior>
  <action>Create SQL contract test first, then implement migration helpers/functions/indexes required for idempotent gateway reconciliation. Wire the new SQL test into `npm run test:sql` after existing checks.</action>
  <verify><automated>npm run test:sql</automated></verify>
  <done>SQL suite proves idempotent reconciliation behavior for settlement/pending/expire paths.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement Midtrans QRIS create + webhook functions</name>
  <files>supabase/functions/create-qris-transaction/index.ts, supabase/functions/midtrans-webhook/index.ts, supabase/functions/_shared/midtrans.ts</files>
  <behavior>
    - Create endpoint returns QR payload for eligible invoice.
    - Webhook verifies signature and updates transaction/invoice state idempotently.
    - Non-eligible invoices and signature mismatch are rejected.
  </behavior>
  <action>Build shared Midtrans client/signature utilities, create resident-authenticated QRIS initiation function, and webhook handler that validates signature (`order_id + status_code + gross_amount + serverKey`) before reconciliation. Persist raw responses/notifications for audit and debugging.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>Backend QRIS flow compiles and enforces integrity checks plus single-source payment state transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resident-client→create-qris-function | Resident-triggered request crosses into gateway-privileged path |
| midtrans→webhook-function | External callback with untrusted payload enters settlement logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | S | `midtrans-webhook` | mitigate | Verify Midtrans signature with server key and reject mismatches before DB updates. |
| T-06-07 | T | reconciliation update path | mitigate | Enforce idempotent update guards keyed by provider transaction/order ID and status transitions. |
| T-06-08 | I | gateway secrets | mitigate | Keep server key only in Edge Function env; never expose to browser bundle. |
</threat_model>

<verification>
- `npm run test:sql`
- `npm run typecheck`
</verification>

<success_criteria>
- QRIS backend can create transactions and reconcile notifications without duplicate payment records.
- Invalid signatures and invalid invoice states are rejected safely.
</success_criteria>

<output>
After completion, create `.planning/phases/06-imports-optional-qris-launch-readiness/06-03-SUMMARY.md`
</output>
