---
phase: 06-imports-optional-qris-launch-readiness
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - supabase/functions/import-preview/index.ts
  - supabase/functions/import-apply/index.ts
  - features/imports/ImportJobsPage.tsx
  - app/admin/imports/page.tsx
  - features/layout/adminNavigation.ts
  - lib/__tests__/importsAdminFlow.test.ts
autonomous: true
requirements: [IMPT-01, IMPT-02, IMPT-03]
must_haves:
  truths:
    - "Admin can open an Imports page, preview CSV validation output, and apply only valid rows."
    - "Each import apply action records import_jobs status and errors for auditability."
  artifacts:
    - path: "app/admin/imports/page.tsx"
      provides: "Admin route for CSV import preview/apply"
    - path: "features/imports/ImportJobsPage.tsx"
      provides: "UI workflow for upload→preview→apply"
    - path: "supabase/functions/import-preview/index.ts"
      provides: "Server-side preview endpoint"
    - path: "supabase/functions/import-apply/index.ts"
      provides: "Transactional apply endpoint"
  key_links:
    - from: "ImportJobsPage.tsx"
      to: "import-preview"
      via: "client.functions.invoke"
      pattern: "functions\.invoke\(\"import-preview\""
    - from: "ImportJobsPage.tsx"
      to: "import-apply"
      via: "client.functions.invoke"
      pattern: "functions\.invoke\(\"import-apply\""
---

<objective>
Wire the import preview/apply workflow into admin operations.

Purpose: Fulfill spreadsheet cutover by making structured imports executable inside the app.
Output: Admin imports route + Edge functions + automated flow coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/imports/importTypes.ts
@lib/imports/importPreview.ts
@features/layout/adminNavigation.ts

<interfaces>
From features/payments/PaymentSubmissionForm.tsx (integration pattern):
```typescript
client.functions.invoke<ResponseType>("function-name", { body: payload })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement Edge functions for preview and apply with import_jobs persistence</name>
  <files>supabase/functions/import-preview/index.ts, supabase/functions/import-apply/index.ts</files>
  <behavior>
    - Preview returns row counts and errors without mutating business tables.
    - Apply writes only rows marked valid and updates `import_jobs` status.
    - Unauthorized roles are rejected consistently.
  </behavior>
  <action>Create `import-preview` and `import-apply` functions using existing `_shared` auth/error helpers. `import-preview` must call the pure preview module from 06-01. `import-apply` must execute each import type in a transaction-safe flow and write `import_jobs` with `draft`→`applied`/`failed` transitions plus structured errors payload.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>Both functions compile, enforce admin-like auth, and persist import job outcome metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add admin imports page and navigation wiring</name>
  <files>features/imports/ImportJobsPage.tsx, app/admin/imports/page.tsx, features/layout/adminNavigation.ts, lib/__tests__/importsAdminFlow.test.ts</files>
  <behavior>
    - Admin sees new “Imports” menu and can access `/admin/imports`.
    - Uploading CSV shows preview counts/errors before apply.
    - Apply button is blocked when invalid rows remain.
  </behavior>
  <action>Implement imports UI in Indonesian matching existing admin card/table patterns. Add explicit import-type selection (kavling, resident mapping, fee override), preview result rendering, and guarded apply action. Update role navigation map so treasurer remains finance-only and imports stay admin/super_admin.</action>
  <verify><automated>npm run test:unit -- importsAdminFlow.test.ts</automated></verify>
  <done>Admin import workflow is reachable, preview-first, and blocks unsafe apply attempts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-browser→Edge Functions | CSV payload crosses into privileged processing |
| Edge Function→Postgres | Bulk write path can impact multiple core tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | E | `import-apply` | mitigate | Enforce JWT + admin/super_admin role checks using shared auth helper before any mutation. |
| T-06-04 | T | import apply SQL writes | mitigate | Apply only validated rows and log rejected row reasons in `import_jobs.errors`; abort invalid batch states. |
| T-06-05 | R | import operations | mitigate | Persist actor, timestamps, counts, and error payload in `import_jobs` for auditable trail. |
</threat_model>

<verification>
- `npm run test:unit -- importsAdminFlow.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- `/admin/imports` exists and provides preview-before-apply flow for all three import types.
- Import apply records outcomes in `import_jobs` and never applies invalid rows.
</success_criteria>

<output>
After completion, create `.planning/phases/06-imports-optional-qris-launch-readiness/06-02-SUMMARY.md`
</output>
