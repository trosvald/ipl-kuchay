---
phase: 01-access-scope-resident-identity
plan: 06
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "05"
files_modified:
  - features/kavlings/KavlingListPage.tsx
  - features/kavlings/KavlingForm.tsx
  - features/residents/ResidentListPage.tsx
  - features/residents/ResidentForm.tsx
  - features/residents/KavlingResidentMapping.tsx
autonomous: true
requirements:
  - AUTH-04
  - KAVL-01
  - KAVL-02
must_haves:
  truths:
    - Admin can manage kavlings and resident assignments without giving that power to treasurer.
    - One resident account can link to multiple kavlings, with each mapping clearly labeled by standardized relation plus optional custom detail.
    - Primary-resident replacement requires an explicit handoff instead of silent overwrite.
  artifacts:
    - path: features/residents/KavlingResidentMapping.tsx
      provides: Explicit resident-to-kavling handoff workflow
      contains: primary
    - path: features/kavlings/KavlingListPage.tsx
      provides: Kavling CRUD aligned with Phase 1 role rules
      contains: Manajemen Kavling
  key_links:
    - from: features/residents/KavlingResidentMapping.tsx
      to: Plan 01 mapping history contract
      via: relation and deactivation/handoff payloads
      pattern: relation|is_primary|active
---

<objective>
Finish the admin resident/kavling management workflows so identity-to-kavling scope stays correct for downstream billing and communication phases.

Purpose: Phase 1 is incomplete unless admins can maintain the kavling and resident assignment truth that all later resident-facing and finance features depend on.
Output: updated resident/kavling CRUD and mapping UI with explicit relation choices, explicit primary handoff, and admin-only control.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
@.planning/phases/01-access-scope-resident-identity/01-RESEARCH.md
@.planning/phases/01-access-scope-resident-identity/01-UI-SPEC.md
@features/kavlings/KavlingListPage.tsx
@features/kavlings/KavlingForm.tsx
@features/residents/ResidentListPage.tsx
@features/residents/ResidentForm.tsx
@features/residents/KavlingResidentMapping.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: tighten resident and mapping form behavior around explicit relation and handoff rules</name>
  <files>features/residents/ResidentForm.tsx, features/residents/KavlingResidentMapping.tsx</files>
  <read_first>
    - features/residents/ResidentForm.tsx
    - features/residents/KavlingResidentMapping.tsx
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
    - docs/plan/milestones/M03-kavlings-residents.md
  </read_first>
  <behavior>
    - Test 1: mapping UI offers standardized relation choices plus custom detail per D-17.
    - Test 2: setting a new primary resident requires an explicit replacement/deactivation path per D-16 and D-26.
    - Test 3: losing all active mappings does not auto-deactivate the resident profile per D-23 and D-24.
  </behavior>
  <action>Refactor the resident admin form and mapping UI to consume the new relation/history contract from Plan 01. `ResidentForm.tsx` should preserve admin-only role/active management while keeping super-admin assignment guarded. `KavlingResidentMapping.tsx` must replace the free-text-only relation input with a standardized relation selector plus a custom-detail field for `other`, support multiple active kavling links per resident per D-13, and require an explicit operator action before a new primary resident replaces an existing one per D-26. Deactivation should record the mapping handoff/ended state without auto-deactivating the resident account per D-23.</action>
  <acceptance_criteria>
    - `features/residents/KavlingResidentMapping.tsx` contains a standardized relation selector
    - `features/residents/KavlingResidentMapping.tsx` contains explicit primary replacement or handoff handling
    - `features/residents/ResidentForm.tsx` still guards `super_admin` assignment behind super-admin-only logic
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Admin mapping forms now reflect the full Phase 1 identity model, including multiple kavlings, standardized relation values, and explicit primary handoff.</done>
</task>

<task type="auto">
  <name>task 2: align resident and kavling CRUD screens with admin-only scope and mapping history</name>
  <files>features/kavlings/KavlingListPage.tsx, features/kavlings/KavlingForm.tsx, features/residents/ResidentListPage.tsx</files>
  <read_first>
    - features/kavlings/KavlingListPage.tsx
    - features/kavlings/KavlingForm.tsx
    - features/residents/ResidentListPage.tsx
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
  </read_first>
  <action>Update the admin CRUD screens so they fully implement AUTH-04, KAVL-01, and KAVL-02 while honoring D-01 through D-04. `KavlingListPage.tsx` and `KavlingForm.tsx` should keep create/edit/activate/deactivate flows but assume treasurer no longer reaches them. `ResidentListPage.tsx` should keep invite/update/deactivate flows, preserve one-account multi-kavling management per D-13 and D-14, and surface mapping status clearly so admins can tell whether a resident is active, unmapped, or former-history-only. Keep audit logging intact for every mutation.</action>
  <acceptance_criteria>
    - `features/kavlings/KavlingListPage.tsx` still supports create/edit/deactivate kavlings
    - `features/residents/ResidentListPage.tsx` still supports invite/update/deactivate residents
    - resident and kavling management screens continue writing audit logs for mutations
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Admin-only resident and kavling operations stay usable after the new Phase 1 role and mapping rules land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → resident/kavling mutation paths | Sensitive mapping and role data is being changed through the UI |
| mapping UI → historical access contract | Handoff/deactivation choices affect what former residents can see later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | Mapping handoff workflow | mitigate | Require an explicit primary replacement/deactivation action so the old primary record is not silently overwritten. |
| T-06-02 | E | Admin mutation screens | mitigate | Keep super-admin assignment checks and rely on Plan 01 backend policies so treasurer cannot regain resident/kavling management power through UI calls. |
| T-06-03 | I | Mapping history state | mitigate | Preserve ended mappings for history use instead of deleting them, so former-resident billing access remains bounded and auditable. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Admin can manage kavlings and resident mappings with the new relation/history rules.
- Treasurer does not regain these screens or powers.
- Primary resident handoff is explicit and auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-scope-resident-identity/01-06-SUMMARY.md`
</output>
