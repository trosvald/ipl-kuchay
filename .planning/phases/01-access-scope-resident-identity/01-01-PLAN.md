---
phase: 01-access-scope-resident-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0012_m07_access_scope_identity.sql
  - supabase/tests/sql/m07_phase1_access_identity.sql
  - package.json
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - PROF-03
  - KAVL-02
must_haves:
  truths:
    - Treasurer can reach finance workflows without gaining resident, kavling, or non-finance settings authority.
    - Residents only see kavling-linked data that belongs to their active or historically eligible occupancy window.
    - Notification preferences exist as category-based records reusable by later in-app and Telegram phases.
  artifacts:
    - path: supabase/migrations/0012_m07_access_scope_identity.sql
      provides: Narrow role helpers, mapping history, notification preference storage, and finance-audit access rules.
      contains: create or replace function public.has_finance_role()
    - path: supabase/tests/sql/m07_phase1_access_identity.sql
      provides: SQL regression coverage for Phase 1 access rules.
      contains: notification_preferences
  key_links:
    - from: supabase/migrations/0012_m07_access_scope_identity.sql
      to: supabase/tests/sql/m07_phase1_access_identity.sql
      via: SQL helper and policy assertions
      pattern: has_finance_role|notification_preferences|kavling_residents
---

<objective>
Harden the Supabase data and policy layer so Phase 1 role boundaries, resident identity history, and preference storage are enforceable by the backend.

Purpose: Phase 1 decisions must survive direct URL access and direct database/API calls, not only UI hiding.
Output: New migration, new SQL regression coverage, and a verified schema/policy contract for downstream UI plans.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
@.planning/phases/01-access-scope-resident-identity/01-RESEARCH.md
@.planning/phases/01-access-scope-resident-identity/01-VALIDATION.md
@docs/plan/milestones/M01-supabase-schema.md
@docs/plan/milestones/M02-auth-roles.md
@docs/plan/milestones/M03-kavlings-residents.md
@supabase/migrations/0002_tables.sql
@supabase/migrations/0004_rls_helpers.sql
@supabase/migrations/0005_rls_policies.sql
@supabase/tests/sql/m01_acceptance_checks.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write SQL regression contract for Phase 1 access rules</name>
  <files>supabase/tests/sql/m07_phase1_access_identity.sql</files>
  <read_first>
    - supabase/tests/sql/m01_acceptance_checks.sql
    - supabase/migrations/0002_tables.sql
    - supabase/migrations/0004_rls_helpers.sql
    - supabase/migrations/0005_rls_policies.sql
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: treasurer can read finance audit rows but cannot update `public.kavlings` or `public.profiles` per D-01, D-02, D-03, D-06.
    - Test 2: a resident with an ended mapping can still read old invoices whose due date falls inside the prior occupancy window per D-23, D-24, D-25.
    - Test 3: notification preferences are stored per category with distinct `telegram_enabled` and `in_app_enabled` flags per D-11 and D-12.
  </behavior>
  <action>Create `supabase/tests/sql/m07_phase1_access_identity.sql` as a self-contained regression script that seeds minimal fixture rows, impersonates the relevant auth contexts, and asserts the new Phase 1 contract before implementation. Cover: (1) treasurer denied non-finance admin writes, (2) admin/super_admin allowed operator writes, (3) historical invoice visibility uses mapping dates, (4) `notification_preferences` enforces one row per `(profile_id, category)`, and (5) second primary-resident replacement requires an explicit deactivation path rather than silent overwrite per D-26. Keep the script safe to run from `npm run test:sql`.</action>
  <acceptance_criteria>
    - `supabase/tests/sql/m07_phase1_access_identity.sql` exists
    - `supabase/tests/sql/m07_phase1_access_identity.sql` contains `notification_preferences`
    - `supabase/tests/sql/m07_phase1_access_identity.sql` contains `treasurer`
    - `supabase/tests/sql/m07_phase1_access_identity.sql` contains `ended_at`
  </acceptance_criteria>
  <verify>
    <automated>supabase db query --file supabase/tests/sql/m07_phase1_access_identity.sql</automated>
  </verify>
  <done>The repository has a failing-then-passable SQL contract that names the Phase 1 access behaviors precisely enough to guide the migration work.</done>
</task>

<task type="auto">
  <name>task 2: implement the Phase 1 migration and wire it into the SQL suite</name>
  <files>supabase/migrations/0012_m07_access_scope_identity.sql, package.json</files>
  <read_first>
    - supabase/migrations/0002_tables.sql
    - supabase/migrations/0004_rls_helpers.sql
    - supabase/migrations/0005_rls_policies.sql
    - supabase/tests/sql/m07_phase1_access_identity.sql
    - docs/plan/milestones/M02-auth-roles.md
    - docs/plan/milestones/M03-kavlings-residents.md
  </read_first>
  <action>Create `supabase/migrations/0012_m07_access_scope_identity.sql` that adds the backend contract required by D-01 through D-06, D-11 through D-17, and D-23 through D-26. Include: `public.has_finance_role()`, `public.has_operator_role()`, `public.can_view_finance_audit_log(target_action text, target_entity_table text)`, and an invoice-history helper that checks active mappings or ended mappings whose `ended_at` still covers the invoice due date. Add `relation_type` + `relation_label` + `started_at` + `ended_at` to `public.kavling_residents`; add `public.notification_preferences(profile_id, category, in_app_enabled, telegram_enabled, created_at, updated_at)`; narrow policies so treasurer retains billing/payment/report/report-audit access but loses resident/kavling/settings/import management; preserve admin/super_admin control; and keep public privacy boundaries intact. Update `package.json` `test:sql` so the new SQL regression file runs in the standard suite.</action>
  <acceptance_criteria>
    - `supabase/migrations/0012_m07_access_scope_identity.sql` contains `create or replace function public.has_finance_role()`
    - `supabase/migrations/0012_m07_access_scope_identity.sql` contains `create or replace function public.has_operator_role()`
    - `supabase/migrations/0012_m07_access_scope_identity.sql` contains `create table public.notification_preferences`
    - `supabase/migrations/0012_m07_access_scope_identity.sql` contains `alter table public.kavling_residents`
    - `package.json` contains `m07_phase1_access_identity.sql`
  </acceptance_criteria>
  <verify>
    <automated>npm run test:sql</automated>
  </verify>
  <done>The Supabase schema and policies now enforce Phase 1 role, mapping-history, and preference rules, and the default SQL suite exercises them.</done>
</task>

<task type="auto">
  <name>task 3: [BLOCKING] push the schema before downstream verification</name>
  <files>supabase/migrations/0012_m07_access_scope_identity.sql</files>
  <read_first>
    - supabase/migrations/0012_m07_access_scope_identity.sql
    - package.json
  </read_first>
  <action>After the migration file is complete, run the mandatory non-interactive schema push for Supabase before any downstream UI plan verifies against the database. Use `supabase db push` if the local project is already linked, otherwise run the repo's reset path so the local database and generated API surface reflect the new migration. Treat any interactive prompt or push failure as blocking and resolve it before proceeding to later plans.</action>
  <acceptance_criteria>
    - `supabase db push` exits 0 or the documented local reset path exits 0
    - `npm run test:sql` exits 0 after the push
  </acceptance_criteria>
  <verify>
    <automated>supabase db push && npm run test:sql</automated>
  </verify>
  <done>The live/local Supabase schema matches the repository migration state, preventing false-positive UI verification against stale database contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase PostgREST/RPC | Untrusted client requests cross into policy-protected data access |
| admin role → finance/admin policies | Treasurers and admins share entry points but require different privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | E | Supabase RLS policies | mitigate | Replace broad `is_admin_like()` write rules with `has_finance_role()` and `has_operator_role()` checks so treasurer cannot mutate resident/kavling/settings data. |
| T-01-02 | I | Resident invoice history scope | mitigate | Add mapping-window helper logic keyed to invoice due date so ended residents see only their own historical rows, never future/new-occupant records. |
| T-01-03 | T | Notification preference storage | mitigate | Enforce unique `(profile_id, category)` rows and authenticated ownership policies on `notification_preferences` to prevent duplicate or cross-account overwrites. |
</threat_model>

<verification>
- `supabase db push`
- `npm run test:sql`
</verification>

<success_criteria>
- Treasurers retain finance access but cannot manage residents, kavlings, imports, or non-finance settings.
- Notification preferences exist as reusable per-category rows.
- Historical resident billing visibility is bounded by explicit mapping dates.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-scope-resident-identity/01-01-SUMMARY.md`
</output>
