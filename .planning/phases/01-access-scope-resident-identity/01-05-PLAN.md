---
phase: 01-access-scope-resident-identity
plan: 05
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - features/layout/AdminShell.tsx
  - features/layout/adminNavigation.ts
  - features/audit/AuditLogPage.tsx
  - app/admin/audit/page.tsx
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - Treasurers only see finance workflows in the admin area.
    - Admins and super admins still see the full operational navigation they are allowed to use.
    - Treasurers can review a finance-only audit slice without seeing broader system audit scope.
  artifacts:
    - path: features/layout/adminNavigation.ts
      provides: Role-scoped admin nav contract
      contains: treasurer
    - path: features/audit/AuditLogPage.tsx
      provides: Finance-only and full-audit rendering paths
      contains: Riwayat aksi sensitif pengurus
  key_links:
    - from: features/layout/AdminShell.tsx
      to: features/layout/adminNavigation.ts
      via: role-scoped nav rendering
      pattern: currentPageTitle|navGroups
---

<objective>
Bring the admin shell and audit UI into alignment with the Phase 1 role boundary decisions.

Purpose: D-01 through D-06 require visible role separation in navigation and audit access, not just backend policy changes hidden from operators.
Output: role-scoped admin navigation and a finance-only audit slice for treasurers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
@.planning/phases/01-access-scope-resident-identity/01-RESEARCH.md
@.planning/phases/01-access-scope-resident-identity/01-UI-SPEC.md
@features/layout/AdminShell.tsx
@features/audit/AuditLogPage.tsx

<interfaces>
From `features/auth/authHooks.ts`:

```typescript
export function useIsAdminLike(): boolean;
export function useIsSuperAdmin(): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: extract a role-scoped admin navigation contract</name>
  <files>features/layout/adminNavigation.ts, features/layout/AdminShell.tsx</files>
  <read_first>
    - features/layout/AdminShell.tsx
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
    - .planning/phases/01-access-scope-resident-identity/01-UI-SPEC.md
  </read_first>
  <action>Create `features/layout/adminNavigation.ts` and move admin nav definitions out of `AdminShell.tsx` so role-scoped rules are explicit and testable. Implement D-01 through D-05 exactly: treasurer gets billing period operations, submissions/payment verification, reporting-related destinations, and finance audit only; treasurer must not get residents, kavlings, imports, communication content, or broad settings links. Admin keeps normal day-to-day operations, and super admin retains elevated controls. Update `AdminShell.tsx` to render only the items returned for the current role while preserving direct-URL protection from Plan 02.</action>
  <acceptance_criteria>
    - `features/layout/adminNavigation.ts` exists
    - `features/layout/adminNavigation.ts` contains a treasurer-specific nav definition
    - `features/layout/AdminShell.tsx` imports from `features/layout/adminNavigation.ts`
    - `features/layout/AdminShell.tsx` no longer hardcodes one universal admin nav for all roles
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Admin navigation is role-aware, and treasurer no longer sees disallowed admin sections.</done>
</task>

<task type="auto">
  <name>task 2: implement the finance-only audit slice inside the admin audit UI</name>
  <files>features/audit/AuditLogPage.tsx, app/admin/audit/page.tsx</files>
  <read_first>
    - features/audit/AuditLogPage.tsx
    - app/admin/audit/page.tsx
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
  </read_first>
  <action>Update `AuditLogPage.tsx` and the `/admin/audit` route so D-06 is enforced end-to-end. Treasurers should see only billing, verification, payment, and report actions/entity tables; admins and super admins should keep the broader audit view. Reuse the backend finance-audit scope introduced in Plan 01 instead of client-side filtering alone. Keep the UI copy in Indonesian and preserve searchable filters, but ensure the scope label makes it clear when the user is viewing the narrower finance audit slice.</action>
  <acceptance_criteria>
    - `features/audit/AuditLogPage.tsx` contains logic for a finance-only audit scope
    - `features/audit/AuditLogPage.tsx` contains billing/payment/report-related filter or label handling
    - `app/admin/audit/page.tsx` still renders `AuditLogPage`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Treasurers can inspect only the finance audit slice they are entitled to, while admins/super admins retain the full audit workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated operator → admin navigation | Visible links must not suggest permissions that backend denies |
| treasurer browser → audit dataset | Finance operators must not receive broader system audit records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | Admin navigation | mitigate | Render role-scoped nav definitions so treasurer cannot discover disallowed sections through standard UI. |
| T-05-02 | E | Audit page data scope | mitigate | Bind the UI to the finance-only backend scope from Plan 01 instead of trusting client-side filters alone. |
| T-05-03 | R | Finance audit access | mitigate | Keep action/entity filters and explicit scope labels so finance operators can explain what subset they are reviewing. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Treasurer admin navigation is finance-only.
- Admin/super-admin navigation remains broader.
- Audit UI honors the narrower treasurer finance slice.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-scope-resident-identity/01-05-SUMMARY.md`
</output>
