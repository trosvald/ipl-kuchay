---
phase: 01-access-scope-resident-identity
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - features/dashboard/PublicDashboardPage.tsx
  - features/resident/ResidentHomePage.tsx
  - features/billing/ResidentInvoicesPage.tsx
  - features/billing/InvoiceDetailPage.tsx
  - app/app/page.tsx
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
  - AUTH-02
must_haves:
  truths:
    - Visitors only see aggregate public billing information for the current public scope.
    - Residents with multiple kavlings see their data grouped per kavling instead of merged into one household total.
    - Former residents can still see their own past invoice history but not future/new-occupant activity.
  artifacts:
    - path: features/dashboard/PublicDashboardPage.tsx
      provides: Aggregate-only public dashboard UX
      contains: Ringkasan Aggregate Publik
    - path: features/resident/ResidentHomePage.tsx
      provides: Resident portal summary grouped by linked kavling
      contains: Kavling Terdaftar
  key_links:
    - from: features/billing/ResidentInvoicesPage.tsx
      to: supabase policy contract from Plan 01
      via: invoice queries filtered by backend history rules
      pattern: from\("invoices"\)
---

<objective>
Align the public and resident portal surfaces with the Phase 1 privacy and scope rules, especially multi-kavling grouping and former-resident billing visibility.

Purpose: even with correct backend policies, residents and visitors need surfaces that present the right scope clearly and never imply broader access.
Output: updated public dashboard, resident home, and invoice views that reflect aggregate-only public data and per-kavling resident data.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
@.planning/phases/01-access-scope-resident-identity/01-RESEARCH.md
@.planning/phases/01-access-scope-resident-identity/01-UI-SPEC.md
@features/dashboard/PublicDashboardPage.tsx
@features/resident/ResidentHomePage.tsx
@features/billing/ResidentInvoicesPage.tsx
@features/billing/InvoiceDetailPage.tsx
@app/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: lock the public dashboard to aggregate-only active-period output</name>
  <files>features/dashboard/PublicDashboardPage.tsx</files>
  <read_first>
    - features/dashboard/PublicDashboardPage.tsx
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
    - docs/plan/milestones/M01-supabase-schema.md
    - .planning/phases/01-access-scope-resident-identity/01-UI-SPEC.md
  </read_first>
  <action>Refine `PublicDashboardPage.tsx` so it explicitly implements PUBL-01 and PUBL-02 per D-05's broader privacy posture: show only aggregate collection data from the safe public RPC, make the current public period summary visually dominant per the UI contract, and keep all copy in Indonesian that states resident names, balances, proofs, and per-kavling payment details are not shown. Do not add resident-level tables, proof links, or any authenticated-only metrics. Keep the page scoped to the public dashboard entry route only.</action>
  <acceptance_criteria>
    - `features/dashboard/PublicDashboardPage.tsx` contains `Ringkasan Aggregate Publik`
    - `features/dashboard/PublicDashboardPage.tsx` contains `Dashboard publik hanya memakai fungsi agregat aman`
    - `features/dashboard/PublicDashboardPage.tsx` does not contain resident-specific identifiers beyond aggregate labels
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>The public dashboard remains open to visitors while clearly communicating that only aggregate collection information is exposed.</done>
</task>

<task type="auto">
  <name>task 2: update resident home and billing views for multi-kavling and former-resident scope</name>
  <files>features/resident/ResidentHomePage.tsx, features/billing/ResidentInvoicesPage.tsx, features/billing/InvoiceDetailPage.tsx, app/app/page.tsx</files>
  <read_first>
    - features/resident/ResidentHomePage.tsx
    - features/billing/ResidentInvoicesPage.tsx
    - features/billing/InvoiceDetailPage.tsx
    - app/app/page.tsx
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
  </read_first>
  <action>Implement the resident-facing scope decisions D-13 through D-16 and D-23 through D-25. `ResidentHomePage.tsx` should keep one-account multi-kavling access but group cards/rows by kavling rather than merging them into a single total. `ResidentInvoicesPage.tsx` and `InvoiceDetailPage.tsx` should present only the rows the backend now authorizes, label them per kavling, and remain read-only for former-resident history access. If the user has zero active mappings but still has historical records, present guidance plus historical billing access instead of an empty broken state. Do not introduce a manual kavling switch as a prerequisite for seeing linked data.</action>
  <acceptance_criteria>
    - `features/resident/ResidentHomePage.tsx` contains kavling-grouped resident content
    - `features/billing/ResidentInvoicesPage.tsx` contains per-kavling labels in the list view
    - `features/billing/InvoiceDetailPage.tsx` still shows invoice details without granting extra mutation scope
    - no file in this task introduces a manual kavling-switch gate before resident data renders
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Resident portal surfaces now show only the right kavling-scoped and history-scoped data, including former-resident billing access without leakage into future occupancy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → public dashboard | Public users must remain limited to aggregate-safe data |
| resident browser → invoice views | Resident-facing screens must not imply or expose neighbor data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | Public dashboard | mitigate | Keep the UI bound to the aggregate RPC output only and reinforce privacy limits in copy and layout. |
| T-03-02 | I | Resident invoice list/detail | mitigate | Render per-kavling labels and trust backend-scoped invoice queries so multi-kavling residents do not see merged or neighbor data. |
| T-03-03 | E | Former-resident history UX | mitigate | Keep historical access read-only and never expose future/current occupant data for ended mappings. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Public visitors see only aggregate collection metrics.
- Multi-kavling residents see grouped data without manual switching.
- Former residents retain past billing visibility only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-scope-resident-identity/01-03-SUMMARY.md`
</output>
