---
phase: 04-announcements-events-resident-home
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - components/ui/textarea.tsx
  - features/announcements/AdminAnnouncementsPage.tsx
  - features/events/AdminEventsPage.tsx
  - app/admin/announcements/page.tsx
  - app/admin/events/page.tsx
  - features/layout/adminNavigation.ts
autonomous: true
requirements: [COMM-02, COMM-03, COMM-04, EVNT-03, EVNT-04]
must_haves:
  truths:
    - "Admin dan super_admin dapat mengelola announcement dengan draft/publish/unpublish/archive lifecycle, urgent toggle, pinned toggle, dan attachment metadata."
    - "Admin dan super_admin dapat mengelola event, membatalkan event tanpa menghapus histori, dan melihat ringkasan RSVP per event."
    - "Tautan admin communication muncul hanya untuk admin dan super_admin; treasurer navigation tetap set keuangan saja."
  artifacts:
    - path: "features/announcements/AdminAnnouncementsPage.tsx"
      provides: "Announcement management page + editor dialog"
    - path: "features/events/AdminEventsPage.tsx"
      provides: "Event management page + RSVP summary"
    - path: "features/layout/adminNavigation.ts"
      provides: "Role-sealed admin navigation entries"
  key_links:
    - from: "AdminAnnouncementsPage"
      to: "announcements + announcement_attachments + announcement-assets"
      via: "Supabase table queries and storage upload/remove flow"
      pattern: "from\(\"announcements\"\)|from\(\"announcement_attachments\"\)|storage\.from\(\"announcement-assets\"\)"
    - from: "AdminEventsPage"
      to: "events + event_attendees"
      via: "Supabase query with RSVP summary aggregation"
      pattern: "from\(\"events\"\)|from\(\"event_attendees\"\)"
---

<objective>
Bangun workspace admin Phase 4 yang terpisah antara pengumuman dan acara, lengkap dengan lifecycle controls, attachment handling, dan RSVP summary, tanpa membocorkan route/aksi ini ke treasurer.

Purpose: Memberi pengurus alur operasional yang jelas dan role-sealed untuk mengelola komunikasi warga.
Output: 2 halaman admin baru + route stubs + update admin navigation + textarea primitive.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-announcements-events-resident-home/04-CONTEXT.md
@.planning/phases/04-announcements-events-resident-home/04-RESEARCH.md
@.planning/phases/04-announcements-events-resident-home/04-UI-SPEC.md
@.planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
@features/billing/BillingPeriodsPage.tsx
@features/payments/AdminSubmissionsPage.tsx
@features/layout/adminNavigation.ts

<interfaces>
From features/layout/adminNavigation.ts:
```typescript
type AdminRole = Extract<AppRole, "treasurer" | "admin" | "super_admin">;
export function getAdminNavigationByRole(role: AppRole | null): NavGroup[];
```

From existing admin page pattern files:
```typescript
const [errorMessage, setErrorMessage] = useState<string | null>(null);
const [loading, setLoading] = useState(true);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement admin announcements management with draft/publish/archive lifecycle</name>
  <read_first>
    - features/payments/AdminSubmissionsPage.tsx
    - features/billing/BillingPeriodsPage.tsx
    - features/layout/adminNavigation.ts
    - .planning/phases/04-announcements-events-resident-home/04-UI-SPEC.md
    - .planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
  </read_first>
  <files>components/ui/textarea.tsx, features/announcements/AdminAnnouncementsPage.tsx, app/admin/announcements/page.tsx</files>
  <behavior>
    - Test 1: save flow defaults to draft and publish is an explicit separate action
    - Test 2: unpublish returns an item to non-resident-visible state without deleting it
    - Test 3: archive keeps the row for history and moves it out of the active resident feed
    - Test 4: urgent and pinned are separate toggles and attachment metadata persists with the announcement
  </behavior>
  <action>Add `components/ui/textarea.tsx` using the approved shadcn textarea primitive, then build `/admin/announcements` as an admin/super_admin-only stateful page following the existing admin page pattern. Match UI-SPEC exactly: heading + helper text, `Pengumuman Baru` action row, status tabs `Draft`, `Terbit`, `Arsip`, list/table rows with title/date/status/urgent/pinned/attachment count, and a dialog editor with fields `title`, `body`, `is_urgent`, `is_pinned`, attachment area, plus footer actions `Simpan Draft`, `Publikasikan`, `Simpan Perubahan`, `Tarik Publikasi`, `Arsipkan`. Use `AlertDialog` for archive and unpublish confirmation, include helper copy that only one urgent hero is shown on resident home per D-07 and UI-SPEC, and keep all resident-facing copy in Indonesian.</action>
  <acceptance_criteria>
    - `components/ui/textarea.tsx` exists
    - `features/announcements/AdminAnnouncementsPage.tsx` contains labels `Pengumuman Baru`, `Simpan Draft`, `Publikasikan`, `Tarik Publikasi`, and `Arsipkan`
    - page includes tabs `Draft`, `Terbit`, and `Arsip`
    - page uses `Dialog` for editor and `AlertDialog` for destructive confirmations
    - page references `announcement-assets` storage flow or attachment metadata handling
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Admin/super_admin can fully manage announcement lifecycle and attachment metadata from a dedicated page that matches the approved UI contract.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement admin events management, RSVP summary, and role-sealed navigation</name>
  <read_first>
    - features/layout/adminNavigation.ts
    - features/payments/AdminSubmissionsPage.tsx
    - .planning/phases/01-access-scope-resident-identity/01-CONTEXT.md
    - .planning/phases/04-announcements-events-resident-home/04-UI-SPEC.md
    - .planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
  </read_first>
  <files>features/events/AdminEventsPage.tsx, app/admin/events/page.tsx, features/layout/adminNavigation.ts</files>
  <behavior>
    - Test 1: event list segments rows into `Mendatang`, `Dibatalkan`, and `Selesai`
    - Test 2: cancel flow keeps the event visible with cancellation note instead of deleting it
    - Test 3: each event shows RSVP totals in the order `Hadir`, `Tidak Hadir`, `Belum Menjawab`
    - Test 4: `Pengumuman` and `Acara` admin nav items appear for `admin` and `super_admin` only, not treasurer
  </behavior>
  <action>Build `/admin/events` as a separate admin/super_admin-only page per D-21 and D-23. Match UI-SPEC exactly: heading + helper text, `Acara Baru` CTA, tabs `Mendatang`, `Dibatalkan`, `Selesai`, per-row fields title/start date/time/location/status/RSVP totals, dialog editor for title/description/location/start/end/cancellation note, and explicit destructive confirm flow `Batalkan Acara`. Show RSVP summary in the order `Hadir`, `Tidak Hadir`, `Belum Menjawab`. Update `features/layout/adminNavigation.ts` to add `/admin/announcements` labeled `Pengumuman` and `/admin/events` labeled `Acara` only for `admin` and `super_admin`; treasurer navigation must remain unchanged from the finance-only set.</action>
  <acceptance_criteria>
    - `features/events/AdminEventsPage.tsx` contains labels `Acara Baru`, `Batalkan Acara`, `Mendatang`, `Dibatalkan`, and `Selesai`
    - `features/layout/adminNavigation.ts` contains `Pengumuman` and `Acara`
    - `features/layout/adminNavigation.ts` does not add those entries to `TREASURER_DASHBOARDS_GROUP`
    - event page renders RSVP totals in the order `Hadir`, `Tidak Hadir`, `Belum Menjawab`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Admin/super_admin can manage event lifecycle and see RSVP summaries from a dedicated page, while treasurer remains excluded from communication/event navigation and controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser admin/super_admin → Supabase tables/storage | Form payloads and attachment operations come from untrusted clients |
| Shared admin shell/navigation → privileged routes | Navigation visibility must not widen access beyond intended roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | T | admin announcement/event payloads | mitigate | Use strict zod contracts from Plan 01 and explicit writable fields in insert/update payload assembly |
| T-04-06 | E | admin communication route exposure | mitigate | Keep route affordances and navigation entries limited to `admin` and `super_admin`; do not add them to treasurer nav |
| T-04-07 | R | lifecycle changes | mitigate | Persist `created_by`/`updated_by`/timestamps and keep non-destructive archive/unpublish/cancel flows auditable |
</threat_model>

<verification>
Run `npm run typecheck && npm run lint` after each task and confirm admin navigation shape with grep/read checks.
</verification>

<success_criteria>
- Admin and super_admin can run announcement and event operations from two separate pages.
- Treasurer still cannot see or use communication/event admin routes from navigation.
- Lifecycle CTAs, tabs, and Indonesian copy match the approved UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/04-announcements-events-resident-home/04-02-SUMMARY.md`
</output>
