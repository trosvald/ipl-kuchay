---
phase: 04-announcements-events-resident-home
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - features/resident/ResidentHomePage.tsx
  - features/announcements/ResidentAnnouncementsPage.tsx
  - features/events/ResidentEventsPage.tsx
  - app/app/announcements/page.tsx
  - app/app/events/page.tsx
  - features/layout/ResidentShell.tsx
autonomous: false
requirements: [COMM-01, EVNT-01, EVNT-02, HOME-01]
must_haves:
  truths:
    - "Resident melihat home terpadu berisi status billing, announcement terbaru, dan upcoming events"
    - "Resident melihat feed announcement yang hanya resident-only published content"
    - "Resident dapat RSVP event dan mengubah pilihannya"
  artifacts:
    - path: "features/resident/ResidentHomePage.tsx"
      provides: "Unified home widgets billing + announcement + event"
    - path: "features/announcements/ResidentAnnouncementsPage.tsx"
      provides: "Resident-only announcement feed"
    - path: "features/events/ResidentEventsPage.tsx"
      provides: "Daftar event upcoming/past + tindakan RSVP"
  key_links:
    - from: "ResidentHomePage"
      to: "announcements/events/invoices queries"
      via: "parallel Supabase fetch"
      pattern: "Promise\.all|from\(\"announcements\"\)|from\(\"events\"\)|from\(\"invoices\"\)"
    - from: "ResidentEventsPage RSVP action"
      to: "event_attendees"
      via: "upsert by event_id + profile_id"
      pattern: "from\(\"event_attendees\"\).*upsert"
---

<objective>
Lengkapi pengalaman resident untuk Phase 4: home terpadu, feed announcement, daftar event, serta aksi RSVP yang bisa diperbarui.

Purpose: Resident mendapat satu pusat informasi harian tanpa bergantung chat manual admin.
Output: 3 halaman resident (home enhanced + announcements + events) dan update navigasi resident.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-access-scope-resident-identity/01-03-SUMMARY.md
@features/resident/ResidentHomePage.tsx
@features/billing/ResidentInvoicesPage.tsx
@features/layout/ResidentShell.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: extend resident home into unified dashboard</name>
  <files>features/resident/ResidentHomePage.tsx, features/layout/ResidentShell.tsx</files>
  <behavior>
    - Test 1: home menampilkan ringkasan tagihan aktif dan count announcement/event terbaru
    - Test 2: kartu urgent announcement diprioritaskan di area atas home
    - Test 3: home tetap menampilkan fallback informatif saat data sebagian gagal dimuat
  </behavior>
  <action>Refactor `ResidentHomePage` agar memuat tiga blok inti HOME-01: billing snapshot (status/tagihan terdekat), announcement terbaru (dengan penekanan urgent), dan upcoming event ringkas. Tambahkan tautan cepat ke `/app/announcements` dan `/app/events` dari shell resident untuk akses lanjutan.</action>
  <verify><automated>npm run typecheck && npm run lint</automated></verify>
  <done>Home resident menjadi dashboard terpadu dan navigasi resident memuat link announcements/events.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: build resident announcements + events pages with RSVP updates</name>
  <files>features/announcements/ResidentAnnouncementsPage.tsx, features/events/ResidentEventsPage.tsx, app/app/announcements/page.tsx, app/app/events/page.tsx</files>
  <behavior>
    - Test 1: feed announcement hanya menampilkan konten terbit, pinned/urgent muncul lebih dulu
    - Test 2: halaman event membagi upcoming dan past dengan detail tanggal/waktu/lokasi/deskripsi
    - Test 3: aksi RSVP (hadir/tidak/ragu) melakukan upsert dan status terbaru terlihat setelah reload
  </behavior>
  <action>Buat dua route resident baru berbasis feature module tipis di `app/app/*`. Halaman announcements harus resident-only feed (COMM-01) dengan dukungan render lampiran (tautan/file metadata). Halaman events harus memenuhi EVNT-01 dan EVNT-02, termasuk tombol ubah RSVP yang aman dan idempoten terhadap baris RSVP resident sendiri.</action>
  <verify><automated>npm run typecheck && npm run lint</automated></verify>
  <done>Resident dapat membaca announcement/event dan memperbarui RSVP miliknya dari UI tanpa error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: resident uat checkpoint for home, feed, dan rsvp</name>
  <action>Jalankan verifikasi manual resident setelah seluruh otomatisasi task 1-2 selesai untuk memastikan pengalaman pengguna akhir sesuai requirement.</action>
  <verify><automated>MISSING — checkpoint manual, lanjutkan setelah task 1-2 lulus `npm run typecheck && npm run lint`</automated></verify>
  <done>Pengguna mengirim sinyal "approved" atau daftar gap yang harus diperbaiki pada iterasi berikutnya.</done>
  <what-built>Unified resident home + announcements feed + events RSVP flow</what-built>
  <how-to-verify>
    1) Login sebagai resident, buka `/app`, pastikan blok tagihan + announcement + event muncul dalam satu layar.
    2) Buka `/app/announcements`, verifikasi item urgent/pinned berada di atas dan lampiran bisa dibuka.
    3) Buka `/app/events`, ubah RSVP (mis. Hadir → Ragu), refresh halaman, pastikan status tetap sesuai perubahan.
  </how-to-verify>
  <resume-signal>Ketik "approved" jika sesuai, atau kirim detail gap yang ditemukan.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser resident → Supabase | Resident dapat memodifikasi input RSVP dari client devtools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | I | resident announcement feed | mitigate | Query hanya kolom yang perlu + rely on RLS published-only visibility |
| T-04-09 | T | RSVP action payload | mitigate | Batasi enum RSVP di validasi client dan enforce policy `profile_id = auth.uid()` |
| T-04-10 | D | resident home data fan-out | accept | Gunakan partial-failure fallback UI agar satu query gagal tidak memblokir seluruh home |
</threat_model>

<verification>
Lulus typecheck/lint dan checkpoint UAT resident pada home/announcement/event/RSVP.
</verification>

<success_criteria>
- HOME-01, COMM-01, EVNT-01, EVNT-02 terlihat nyata di portal resident.
- Alur RSVP update tervalidasi oleh user dan tetap konsisten setelah reload.
</success_criteria>

<output>
After completion, create `.planning/phases/04-announcements-events-resident-home/04-03-SUMMARY.md`
</output>
