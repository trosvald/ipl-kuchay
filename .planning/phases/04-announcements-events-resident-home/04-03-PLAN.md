---
phase: 04-announcements-events-resident-home
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - features/resident/ResidentHomePage.tsx
  - features/layout/ResidentShell.tsx
  - features/announcements/ResidentAnnouncementsPage.tsx
  - features/announcements/ResidentAnnouncementDetailPage.tsx
  - features/events/ResidentEventsPage.tsx
  - features/events/ResidentEventDetailPage.tsx
  - app/app/announcements/page.tsx
  - app/app/announcements/[id]/page.tsx
  - app/app/events/page.tsx
  - app/app/events/[id]/page.tsx
autonomous: true
requirements: [COMM-01, EVNT-01, EVNT-02, HOME-01]
must_haves:
  truths:
    - "Resident membuka `/app` dan melihat dashboard terpadu dengan urutan billing first, urgent hero, latest announcements, lalu upcoming events."
    - "Resident melihat feed pengumuman resident-only lengkap dengan hero urgent tunggal, preview cards, detail page, dan riwayat arsip."
    - "Resident melihat event mendatang/dibatalkan/selesai terpisah, membaca detail acara, dan memperbarui RSVP miliknya sampai acara dimulai."
  artifacts:
    - path: "features/resident/ResidentHomePage.tsx"
      provides: "Unified resident dashboard"
    - path: "features/announcements/ResidentAnnouncementsPage.tsx"
      provides: "Resident announcement feed + archive split"
    - path: "features/announcements/ResidentAnnouncementDetailPage.tsx"
      provides: "Resident announcement detail"
    - path: "features/events/ResidentEventsPage.tsx"
      provides: "Resident events tabs and summary cards"
    - path: "features/events/ResidentEventDetailPage.tsx"
      provides: "Resident event detail + RSVP segmented control"
  key_links:
    - from: "ResidentHomePage"
      to: "invoices, announcements, events"
      via: "parallel fetch with per-slice fallback"
      pattern: "Promise\.all|from\(\"invoices\"\)|from\(\"announcements\"\)|from\(\"events\"\)"
    - from: "ResidentAnnouncementsPage"
      to: "ResidentAnnouncementDetailPage"
      via: "next/link CTA `Baca Pengumuman`"
      pattern: "Baca Pengumuman"
    - from: "ResidentEventDetailPage RSVP control"
      to: "event_attendees"
      via: "upsert of the authenticated resident row"
      pattern: "from\(\"event_attendees\"\).*upsert"
---

<objective>
Lengkapi pengalaman resident Phase 4: dashboard `/app` yang billing-first, feed/detail pengumuman, feed/detail acara, dan RSVP yang bisa diperbarui sampai acara dimulai.

Purpose: Resident mendapat command center harian yang terpercaya tanpa berubah menjadi social feed.
Output: Enhanced resident home + resident announcement routes + resident event routes + resident nav updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-announcements-events-resident-home/04-CONTEXT.md
@.planning/phases/04-announcements-events-resident-home/04-RESEARCH.md
@.planning/phases/04-announcements-events-resident-home/04-UI-SPEC.md
@.planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
@.planning/phases/02-billing-configuration-resident-billing-view/02-03-SUMMARY.md
@features/resident/ResidentHomePage.tsx
@features/billing/ResidentInvoicesPage.tsx
@features/layout/ResidentShell.tsx

<interfaces>
From features/billing/ResidentInvoicesPage.tsx:
```typescript
function groupInvoicesByKavling(invoices: ResidentInvoiceRow[]): KavlingGroup[]
function ArrearsSummaryCard({ totalOverdue, overdueCount }: { totalOverdue: number; overdueCount: number })
```

From features/layout/ResidentShell.tsx:
```typescript
export function ResidentShell({ children }: Readonly<{ children: React.ReactNode }>)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: refactor `/app` into the approved resident command-center dashboard</name>
  <read_first>
    - features/resident/ResidentHomePage.tsx
    - features/billing/ResidentInvoicesPage.tsx
    - features/layout/ResidentShell.tsx
    - .planning/phases/02-billing-configuration-resident-billing-view/02-03-SUMMARY.md
    - .planning/phases/04-announcements-events-resident-home/04-UI-SPEC.md
  </read_first>
  <files>features/resident/ResidentHomePage.tsx, features/layout/ResidentShell.tsx</files>
  <behavior>
    - Test 1: `/app` renders sections in the locked order `Ringkasan Tagihan` → urgent hero/fallback hero → `Pengumuman Terbaru` → `Acara Mendatang`
    - Test 2: multi-kavling billing remains per-kavling cards with no merged household total
    - Test 3: announcement or event fetch failure shows an inline error card for that slice while billing still renders
    - Test 4: resident nav adds `Pengumuman` and `Acara` after `Tagihan` and before `Pengaturan`
  </behavior>
  <action>Refactor `ResidentHomePage.tsx` away from the current kavling table + placeholder queue into the UI-SPEC dashboard. Keep billing first per D-01 to D-04 by summarizing real invoice truth from the existing resident billing patterns: one card per kavling showing kavling code, outstanding/arrears summary, unpaid invoice count, nearest due date, and CTA `Lihat Tagihan`. Then render exactly one non-billing hero: urgent published announcement first, otherwise newest published announcement without urgent tint. Below that, render `Pengumuman Terbaru` with at most two preview cards and `Acara Mendatang` with at most three upcoming cards. Use skeleton cards for loading, neutral empty cards for no-data states, and inline error cards with `Muat Ulang` action for partial failures. Update `ResidentShell.tsx` navigation so `/app` remains `Ringkasan`, `Pengumuman` and `Acara` appear after `Tagihan`, and all labels stay in Indonesian.</action>
  <acceptance_criteria>
    - `features/resident/ResidentHomePage.tsx` contains headings `Ringkasan Tagihan`, `Pengumuman Terbaru`, and `Acara Mendatang`
    - file contains CTA `Lihat Tagihan`
    - file renders no merged household total above per-kavling cards
    - `features/layout/ResidentShell.tsx` contains `Pengumuman` and `Acara` links between `Invoice/Tagihan` and `Pengaturan`
    - primary loading state for main sections uses `Skeleton`, not plain `Memuat...`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>The resident home becomes the approved billing-first dashboard and resident navigation exposes the new Phase 4 pages.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: build resident announcement feed and detail routes with urgent hero and archive history</name>
  <read_first>
    - features/layout/ResidentShell.tsx
    - .planning/phases/04-announcements-events-resident-home/04-UI-SPEC.md
    - .planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
  </read_first>
  <files>features/announcements/ResidentAnnouncementsPage.tsx, features/announcements/ResidentAnnouncementDetailPage.tsx, app/app/announcements/page.tsx, app/app/announcements/[id]/page.tsx</files>
  <behavior>
    - Test 1: feed shows at most one urgent hero and all remaining cards newest-first with `Penting` badge only where relevant
    - Test 2: resident feed excludes unpublished rows and splits active feed from `Riwayat Pengumuman`
    - Test 3: detail route shows title, publish date, urgency/archive badges, attachment chips, and body inside a readable card container
  </behavior>
  <action>Build resident announcement routes exactly as specified by UI-SPEC and D-07 to D-13. `ResidentAnnouncementsPage.tsx` must render one optional urgent hero, then active `Pengumuman` cards with publish date, urgency badge, title, 2-line preview, attachment chips, and CTA `Baca Pengumuman`, followed by muted `Riwayat Pengumuman` archive cards. Create `ResidentAnnouncementDetailPage.tsx` for `/app/announcements/[id]` with back link, title, publish date, urgency/archive badges, attachment chips or thumbnails, and archive note `Pengumuman ini berada di arsip dan disimpan sebagai riwayat informasi warga.` when applicable. Use only resident-visible published/archived data from Plan 01; do not query drafts and then filter them client-side.</action>
  <acceptance_criteria>
    - `features/announcements/ResidentAnnouncementsPage.tsx` contains headings `Pengumuman` and `Riwayat Pengumuman`
    - feed contains CTA `Baca Pengumuman`
    - feed contains empty-state copy `Belum ada pengumuman`
    - detail page contains archive note string from UI-SPEC
    - route files `app/app/announcements/page.tsx` and `app/app/announcements/[id]/page.tsx` exist and are thin wrappers
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Residents can browse current and archived announcements and open a full detail page with attachments and official-notice styling.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: build resident events feed and detail routes with editable RSVP until event start</name>
  <read_first>
    - .planning/phases/04-announcements-events-resident-home/04-UI-SPEC.md
    - .planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
    - lib/validation.ts
  </read_first>
  <files>features/events/ResidentEventsPage.tsx, features/events/ResidentEventDetailPage.tsx, app/app/events/page.tsx, app/app/events/[id]/page.tsx</files>
  <behavior>
    - Test 1: events page uses tabs in exact order `Mendatang`, `Dibatalkan`, `Selesai`
    - Test 2: cards show date, time, location, short description, current RSVP state, and main CTA before opening detail
    - Test 3: detail RSVP control offers `Saya Hadir`, `Tidak Bisa Hadir`, `Belum Menjawab` in that exact order and persists a resident-owned upsert
    - Test 4: RSVP becomes read-only with the exact locked copy when event has started or is cancelled
  </behavior>
  <action>Build resident event routes to satisfy D-14 to D-18 and UI-SPEC exactly. `ResidentEventsPage.tsx` must render tabs `Mendatang`, `Dibatalkan`, `Selesai`, optional count badges, and event cards with in-list date/time/location/short description/current RSVP/main CTA `Lihat Detail Acara`. Create `ResidentEventDetailPage.tsx` for `/app/events/[id]` with title, time range, location, status badge, description, and a 3-choice segmented RSVP control in this exact order: `Saya Hadir`, `Tidak Bisa Hadir`, `Belum Menjawab`. Upsert RSVP only for the authenticated resident row. When the event has started or finished, show `RSVP sudah ditutup karena acara telah dimulai.` and disable edits; when cancelled, show `Acara ini dibatalkan. RSVP tidak dapat diubah.` and disable edits. After successful RSVP change, show inline confirmation near the control instead of relying on global toast only.</action>
  <acceptance_criteria>
    - `features/events/ResidentEventsPage.tsx` contains `Mendatang`, `Dibatalkan`, and `Selesai`
    - feed contains CTA `Lihat Detail Acara`
    - detail page contains `Saya Hadir`, `Tidak Bisa Hadir`, and `Belum Menjawab` in the RSVP control
    - detail page contains the exact locked copy `RSVP sudah ditutup karena acara telah dimulai.`
    - route files `app/app/events/page.tsx` and `app/app/events/[id]/page.tsx` exist and are thin wrappers
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Residents can browse event states, open event detail, and update only their own RSVP until the event start cutoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser resident → Supabase | Resident can tamper with event/announcement requests and RSVP payloads from the client |
| Resident dashboard multi-fetch → UI slices | One data source failing must not suppress unrelated trusted billing data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | I | resident announcement and event reads | mitigate | Query only resident-visible rows under RLS and expose only needed columns/attachment metadata |
| T-04-09 | T | RSVP UI action | mitigate | Use the strict RSVP schema and rely on backend self-row policy enforcement for the final write boundary |
| T-04-10 | D | unified resident home fan-out | mitigate | Keep billing, announcement, and event slices independently recoverable with per-slice loading/error cards |
</threat_model>

<verification>
Run `npm run typecheck && npm run lint` after each task; use file reads/grep to confirm locked UI copy and route presence.
</verification>

<success_criteria>
- Resident `/app` follows the approved billing-first information hierarchy.
- Residents can browse full announcement and event surfaces from dedicated routes.
- RSVP editing stays available only until the event starts and shows the locked Indonesian copy when closed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-announcements-events-resident-home/04-03-SUMMARY.md`
</output>
