---
phase: 04-announcements-events-resident-home
plan: 04
type: execute
wave: 1
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - features/auth/authHooks.ts
  - features/auth/RequireOperatorRole.tsx
  - app/admin/announcements/page.tsx
  - app/admin/events/page.tsx
  - features/announcements/AdminAnnouncementsPage.tsx
  - features/announcements/ResidentAnnouncementDetailPage.tsx
autonomous: true
requirements: [COMM-02, COMM-03, COMM-04, EVNT-03]
gap_closure: true
must_haves:
  truths:
    - "Admin dan super_admin dapat mengakses halaman komunikasi/admin konten, tetapi treasurer ditolak walau mencoba URL langsung."
    - "Admin dapat mengunggah dan menghapus lampiran pengumuman ke bucket private announcement-assets dari halaman admin."
    - "Resident dapat membuka atau mengunduh lampiran pengumuman dari halaman detail, bukan hanya melihat badge label."
  artifacts:
    - path: "features/auth/RequireOperatorRole.tsx"
      provides: "Guard admin/super_admin-only untuk workspace komunikasi"
    - path: "features/announcements/AdminAnnouncementsPage.tsx"
      provides: "Upload + delete flow untuk announcement-assets"
    - path: "features/announcements/ResidentAnnouncementDetailPage.tsx"
      provides: "Resident-safe attachment open/download affordances"
  key_links:
    - from: "app/admin/announcements/page.tsx"
      to: "features/auth/RequireOperatorRole.tsx"
      via: "route wrapper"
      pattern: "RequireOperatorRole"
    - from: "features/announcements/AdminAnnouncementsPage.tsx"
      to: "announcement-assets + announcement_attachments"
      via: "storage upload/delete and attachment row persistence"
      pattern: "storage\.from\(\"announcement-assets\"\)|from\(\"announcement_attachments\"\)"
    - from: "features/announcements/ResidentAnnouncementDetailPage.tsx"
      to: "announcement-assets"
      via: "signed URL open/download action"
      pattern: "createSignedUrl|openSignedArtifactUrl|Buka Lampiran|Unduh Lampiran"
---

<objective>
Tutup dua gap pengumuman/admin yang masih terbuka: role guard komunikasi harus benar-benar admin/super_admin-only, dan lampiran pengumuman harus end-to-end dari upload admin sampai akses resident.

Purpose: Menyelesaikan `COMM-04` dan boundary D-23 tanpa membiarkan treasurer masuk ke workspace komunikasi lewat URL langsung.
Output: Guard operator-only baru, route wrappers komunikasi yang diperketat, upload/delete lampiran admin, dan resident attachment actions yang usable.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-announcements-events-resident-home/04-CONTEXT.md
@.planning/phases/04-announcements-events-resident-home/04-VERIFICATION.md
@.planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
@.planning/phases/04-announcements-events-resident-home/04-02-SUMMARY.md
@.planning/phases/04-announcements-events-resident-home/04-03-SUMMARY.md
@features/auth/authHooks.ts
@features/auth/RequireAdminLike.tsx
@features/payments/ProofPreviewButton.tsx
@lib/privateArtifact.ts

<interfaces>
From features/auth/authHooks.ts:
```typescript
export function useIsAdminLike(): boolean;
export function useIsSuperAdmin(): boolean;
```

From features/payments/ProofPreviewButton.tsx + lib/privateArtifact.ts:
```typescript
const { data, error } = await client.functions.invoke<SignedUrlResponse>(...)
export async function openSignedArtifactUrl(signedUrl: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: seal communication routes to admin and super_admin only</name>
  <read_first>
    - features/auth/authHooks.ts
    - features/auth/RequireAdminLike.tsx
    - app/admin/announcements/page.tsx
    - app/admin/events/page.tsx
    - .planning/phases/04-announcements-events-resident-home/04-CONTEXT.md
    - .planning/phases/04-announcements-events-resident-home/04-VERIFICATION.md
  </read_first>
  <files>features/auth/authHooks.ts, features/auth/RequireOperatorRole.tsx, app/admin/announcements/page.tsx, app/admin/events/page.tsx</files>
  <action>Add `useIsOperatorRole()` that returns `true` only for `role === "admin" || role === "super_admin"` so D-23 is enforced in code, not only navigation. Create `RequireOperatorRole.tsx` by copying the existing guard pattern from `RequireAdminLike.tsx` but redirecting unauthorized users to `/admin` and showing operator-only copy. Update `app/admin/announcements/page.tsx` and `app/admin/events/page.tsx` to wrap their existing feature-page exports in `RequireOperatorRole`, leaving `app/admin/layout.tsx` unchanged so finance pages still allow treasurer. Do not broaden `useIsAdminLike()` or finance guards; this fix must be route-specific for communication pages.</action>
  <acceptance_criteria>
    - `features/auth/authHooks.ts` contains `export function useIsOperatorRole()`
    - `features/auth/RequireOperatorRole.tsx` exists and imports `useIsOperatorRole`
    - `app/admin/announcements/page.tsx` contains `RequireOperatorRole`
    - `app/admin/events/page.tsx` contains `RequireOperatorRole`
    - no change removes treasurer access from unrelated finance/admin pages via `app/admin/layout.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && rg -n "useIsOperatorRole|RequireOperatorRole" features/auth/authHooks.ts features/auth/RequireOperatorRole.tsx app/admin/announcements/page.tsx app/admin/events/page.tsx</automated>
  </verify>
  <done>Manual URL access to `/admin/announcements` and `/admin/events` is blocked for treasurer while admin and super_admin retain access.</done>
</task>

<task type="auto">
  <name>task 2: complete announcement attachment flow for admin upload and resident access</name>
  <read_first>
    - features/announcements/AdminAnnouncementsPage.tsx
    - features/announcements/ResidentAnnouncementDetailPage.tsx
    - features/payments/ProofPreviewButton.tsx
    - lib/privateArtifact.ts
    - supabase/migrations/0019_m08_announcements_events.sql
    - .planning/phases/04-announcements-events-resident-home/04-CONTEXT.md
    - .planning/phases/04-announcements-events-resident-home/04-VERIFICATION.md
  </read_first>
  <files>features/announcements/AdminAnnouncementsPage.tsx, features/announcements/ResidentAnnouncementDetailPage.tsx</files>
  <action>Replace the Phase 4 placeholder with a real attachment flow for `COMM-04`. In `AdminAnnouncementsPage.tsx`, add a file input that accepts `image/png,image/jpeg,image/gif,application/pdf`; when an announcement already has an `editor.id`, upload files to `announcement-assets` under `announcements/${editor.id}/{uuid}-{originalName}`, then insert matching `announcement_attachments` rows with `announcement_id`, `label`, `storage_path`, `mime_type`, and `size_bytes`. When deleting, remove both the table row and the storage object. For unsaved announcements, do not promise a future version; instead show concrete helper copy telling the operator to save the draft first before adding lampiran. In `ResidentAnnouncementDetailPage.tsx`, replace label-only badges with clear resident affordances per D-11: image attachments render a small thumbnail/open action, non-image attachments render `Buka Lampiran` or `Unduh Lampiran` buttons. Generate a short-lived signed URL with `client.storage.from("announcement-assets").createSignedUrl(att.storage_path, 60)` and open it via `openSignedArtifactUrl`, keeping the bucket private.</action>
  <acceptance_criteria>
    - `features/announcements/AdminAnnouncementsPage.tsx` contains `announcement-assets`
    - `features/announcements/AdminAnnouncementsPage.tsx` contains `type="file"`
    - `features/announcements/AdminAnnouncementsPage.tsx` no longer contains `versi selanjutnya`
    - `features/announcements/ResidentAnnouncementDetailPage.tsx` contains `createSignedUrl`
    - `features/announcements/ResidentAnnouncementDetailPage.tsx` contains `Buka Lampiran` or `Unduh Lampiran`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && rg -n "announcement-assets|type=\"file\"|createSignedUrl|Buka Lampiran|Unduh Lampiran|versi selanjutnya" features/announcements/AdminAnnouncementsPage.tsx features/announcements/ResidentAnnouncementDetailPage.tsx</automated>
  </verify>
  <done>Admin can upload/remove announcement attachments from the private bucket and residents can open/download those attachments from announcement detail pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser admin/super_admin → Supabase storage/tables | File uploads and attachment metadata come from untrusted clients |
| Browser resident → private announcement assets | Resident file access must stay policy-checked and time-limited |
| Shared `/admin` shell → communication routes | Finance-role users must not inherit communication permissions from broad layout guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | E | communication route guard | mitigate | Add `RequireOperatorRole` and wrap only communication route stubs so treasurer cannot reach those URLs directly |
| T-04-12 | T | attachment upload metadata | mitigate | Persist only explicit `announcement_attachments` fields after storage upload and keep accepted MIME list limited to png/jpeg/gif/pdf |
| T-04-13 | I | resident attachment access | mitigate | Keep bucket private and use short-lived signed URLs opened through `openSignedArtifactUrl`; never expose public bucket URLs |
</threat_model>

<verification>
Run typecheck, then grep for the new operator guard wiring and real attachment storage/signed-URL flow.
</verification>

<success_criteria>
- Treasurer is denied direct access to announcement/event admin routes.
- The admin announcement page no longer contains a deferred upload placeholder.
- Resident announcement detail exposes usable file/image actions backed by private signed URLs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-announcements-events-resident-home/04-04-SUMMARY.md`
</output>
