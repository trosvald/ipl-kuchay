---
phase: 04-announcements-events-resident-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0013_m08_announcements_events.sql
  - supabase/tests/sql/m08_announcements_events_access.sql
  - lib/validation.ts
  - lib/__tests__/validation.test.ts
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03, COMM-04, EVNT-01, EVNT-02, EVNT-03, EVNT-04]
must_haves:
  truths:
    - "Resident hanya bisa membaca pengumuman terbit dan event yang memang boleh diakses"
    - "Admin/treasurer/super_admin dapat mengelola pengumuman dan event tanpa bypass keamanan browser"
    - "Resident bisa RSVP dan memperbarui pilihannya tanpa mengubah RSVP milik akun lain"
  artifacts:
    - path: "supabase/migrations/0013_m08_announcements_events.sql"
      provides: "Tabel pengumuman/event/RSVP, RLS, helper SQL, dan trigger"
    - path: "supabase/tests/sql/m08_announcements_events_access.sql"
      provides: "Regresi SQL untuk akses resident vs admin dan guard RSVP"
    - path: "lib/validation.ts"
      provides: "Schema zod untuk payload announcement/event/RSVP"
  key_links:
    - from: "RLS policies"
      to: "auth.uid() + role helpers"
      via: "policy USING/WITH CHECK"
      pattern: "has_finance_role|has_operator_role|auth.uid\(\)"
    - from: "RSVP upsert logic"
      to: "event_attendees unique(event_id, profile_id)"
      via: "on conflict update"
      pattern: "event_id, profile_id"
---

<objective>
Buat kontrak backend announcements/events yang aman sejak awal: skema data, RLS, helper query, dan validasi payload agar UI resident/admin pada wave berikutnya tinggal konsumsi API yang konsisten.

Purpose: Mengunci integritas data komunikasi warga dan mencegah kebocoran lintas akun.
Output: Migrasi SQL + SQL regression test + schema validasi TS.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/0002_tables.sql
@supabase/migrations/0012_m07_access_scope_identity.sql
@lib/validation.ts
@supabase/tests/sql/m07_phase1_access_identity.sql
</context>

<tasks>
<task type="auto">
  <name>task 1: tambah model announcement, event, RSVP + RLS</name>
  <files>supabase/migrations/0013_m08_announcements_events.sql</files>
  <action>Buat tabel `announcements`, `announcement_attachments`, `events`, dan `event_attendees` lengkap dengan FK ke `profiles`, status/lifecycle kolom (publish/unpublish/archive untuk announcement; cancel untuk event), atribut urgensi/pinning, serta metadata berkas lampiran. Tambahkan RLS: resident hanya SELECT konten published + tidak archived + event visible; admin-like (treasurer/admin/super_admin) boleh kelola sesuai kebutuhan requirement. Pastikan resident RSVP hanya untuk `profile_id = auth.uid()` dan tidak bisa menulis RSVP milik orang lain.</action>
  <verify><automated>supabase db reset --yes && supabase db query --file supabase/tests/sql/m08_announcements_events_access.sql</automated></verify>
  <done>Tabel + policy aktif, dan skenario access control resident/admin pada SQL test lulus.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: tambah kontrak validasi payload announcement/event/rsvp</name>
  <files>lib/validation.ts, lib/__tests__/validation.test.ts</files>
  <behavior>
    - Test 1: payload announcement reject bila title/body kosong atau terlalu panjang
    - Test 2: payload event reject bila tanggal selesai &lt; tanggal mulai
    - Test 3: payload RSVP hanya menerima status enum yang diizinkan
  </behavior>
  <action>Tambahkan schema zod untuk create/update announcement, lampiran announcement, create/update event, dan RSVP update. Ikuti pola strict writable fields seperti Phase 01 agar tidak ada field sensitif ikut ter-accept dari client.</action>
  <verify><automated>npm run test:unit -- lib/__tests__/validation.test.ts</automated></verify>
  <done>Semua schema baru memiliki test pass dan menolak payload invalid sesuai behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser resident → Supabase tables | Input RSVP dan baca feed berasal dari client tidak terpercaya |
| Browser admin-like → Supabase tables | Mutasi announcement/event harus tetap dijaga oleh RLS, bukan kepercayaan UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | E | announcement/event write path | mitigate | RLS WITH CHECK berbasis role helper; resident tidak punya policy write announcement/event |
| T-04-02 | I | resident feed query | mitigate | Policy SELECT membatasi published + non-archived dan scope resident auth |
| T-04-03 | T | event_attendees RSVP row | mitigate | Unique `(event_id, profile_id)` + policy update hanya `profile_id = auth.uid()` |
| T-04-04 | R | admin content changes | mitigate | Simpan created_by/updated_by dan timestamp agar jejak perubahan dapat diaudit |
</threat_model>

<verification>
Jalankan SQL regression baru dan unit test zod schema.
</verification>

<success_criteria>
- Model data komunikasi + event siap dipakai UI resident/admin.
- Kebijakan akses resident/admin-like tervalidasi oleh SQL test.
- Kontrak input client tervalidasi dengan unit test.
</success_criteria>

<output>
After completion, create `.planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md`
</output>
