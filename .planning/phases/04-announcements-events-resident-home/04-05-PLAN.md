---
phase: 04-announcements-events-resident-home
plan: 05
type: execute
wave: 1
depends_on: [04-01, 04-03]
files_modified:
  - features/events/ResidentEventDetailPage.tsx
  - supabase/tests/sql/m08_announcements_events_access.sql
  - .planning/phases/04-announcements-events-resident-home/04-01-PLAN.md
  - .planning/phases/04-announcements-events-resident-home/04-RESEARCH.md
autonomous: true
requirements: [EVNT-02]
gap_closure: true
must_haves:
  truths:
    - "Resident dapat menyimpan dan memperbarui RSVP miliknya sendiri sampai acara dimulai tanpa error schema mismatch."
    - "SQL regression Phase 4 benar-benar menjalankan insert/update RSVP terhadap schema live, bukan hanya memeriksa policy strings."
    - "Dokumen plan/research Phase 4 menunjuk ke artifact migrasi yang benar (`0019_m08_announcements_events.sql`)."
  artifacts:
    - path: "features/events/ResidentEventDetailPage.tsx"
      provides: "RSVP upsert payload yang cocok dengan schema event_attendees"
    - path: "supabase/tests/sql/m08_announcements_events_access.sql"
      provides: "Real-schema RSVP insert/update regression"
    - path: ".planning/phases/04-announcements-events-resident-home/04-01-PLAN.md"
      provides: "Traceable must-have artifact path untuk migrasi Phase 4"
  key_links:
    - from: "features/events/ResidentEventDetailPage.tsx"
      to: "public.event_attendees"
      via: "upsert payload without nonexistent columns"
      pattern: "from\(\"event_attendees\"\).*upsert"
    - from: "supabase/tests/sql/m08_announcements_events_access.sql"
      to: "public.event_attendees"
      via: "actual insert/update assertions against live schema"
      pattern: "insert into public\.event_attendees|update public\.event_attendees|on conflict"
    - from: ".planning/phases/04-announcements-events-resident-home/04-01-PLAN.md"
      to: "supabase/migrations/0019_m08_announcements_events.sql"
      via: "artifact and verify references"
      pattern: "0019_m08_announcements_events\.sql"
---

<objective>
Perbaiki gap RSVP runtime dan kembalikan traceability artifact Phase 4 supaya verifikasi berikutnya melihat kontrak backend yang benar-benar bisa dijalankan dan ditelusuri.

Purpose: Menutup `EVNT-02` dan menghilangkan false gap dokumentasi akibat referensi migrasi `0013` yang sudah direnumber menjadi `0019`.
Output: RSVP payload sinkron dengan schema live, SQL regression RSVP yang runnable, dan dokumen plan/research yang menunjuk ke migrasi benar.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-announcements-events-resident-home/04-CONTEXT.md
@.planning/phases/04-announcements-events-resident-home/04-VERIFICATION.md
@.planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
@.planning/phases/04-announcements-events-resident-home/04-03-SUMMARY.md
@features/events/ResidentEventDetailPage.tsx
@supabase/migrations/0019_m08_announcements_events.sql
@supabase/tests/sql/m08_announcements_events_access.sql

<interfaces>
From features/events/ResidentEventDetailPage.tsx:
```typescript
type RSVPResponse = "attending" | "not_attending" | "no_response";
const { profile } = useAuth();
```

From supabase/migrations/0019_m08_announcements_events.sql:
```sql
create table public.event_attendees (
  event_id uuid not null,
  profile_id uuid not null,
  response rsvp_response not null default 'no_response',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (event_id, profile_id)
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: align resident RSVP writes with the real event_attendees schema</name>
  <read_first>
    - features/events/ResidentEventDetailPage.tsx
    - supabase/migrations/0019_m08_announcements_events.sql
    - supabase/tests/sql/m08_announcements_events_access.sql
    - .planning/phases/04-announcements-events-resident-home/04-CONTEXT.md
    - .planning/phases/04-announcements-events-resident-home/04-VERIFICATION.md
  </read_first>
  <files>features/events/ResidentEventDetailPage.tsx, supabase/tests/sql/m08_announcements_events_access.sql</files>
  <behavior>
    - Test 1: resident can insert an RSVP row with only `event_id`, `profile_id`, and `response` before event start
    - Test 2: resident can update that same row before event start using the live unique `(event_id, profile_id)` conflict target
    - Test 3: resident cannot update another resident's RSVP or mutate after the event-start cutoff
  </behavior>
  <action>Fix the `EVNT-02` write path by removing `responded_at` from the `event_attendees` upsert payload in `ResidentEventDetailPage.tsx`; the client payload must send only `event_id`, `profile_id`, and `response`, because the live schema in `0019_m08_announcements_events.sql` has no `responded_at` column. Keep the existing `onConflict: "event_id,profile_id"`, success message, and locked-state copy from D-14/D-15. Extend `supabase/tests/sql/m08_announcements_events_access.sql` beyond policy-string checks: seed a future event, perform a real resident insert into `public.event_attendees`, perform a real resident update for the same row, assert the stored `response` changes, then assert post-start and wrong-profile mutations fail under RLS. Use concrete SQL statements (`insert ... on conflict ... do update set response = excluded.response`) so the regression would fail if the schema contract drifts again.</action>
  <acceptance_criteria>
    - `features/events/ResidentEventDetailPage.tsx` no longer contains `responded_at`
    - RSVP upsert still targets `event_attendees` with `onConflict: "event_id,profile_id"`
    - `supabase/tests/sql/m08_announcements_events_access.sql` contains a real `insert into public.event_attendees`
    - SQL regression contains a real update or upsert path asserting the response changed
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && supabase db query --file supabase/tests/sql/m08_announcements_events_access.sql</automated>
  </verify>
  <done>The resident RSVP control writes a payload accepted by the live schema, and the SQL regression proves insert/update behavior against the real database contract.</done>
</task>

<task type="auto">
  <name>task 2: restore migration artifact traceability to the implemented Phase 4 backend contract</name>
  <read_first>
    - .planning/phases/04-announcements-events-resident-home/04-01-PLAN.md
    - .planning/phases/04-announcements-events-resident-home/04-RESEARCH.md
    - .planning/phases/04-announcements-events-resident-home/04-VERIFICATION.md
    - .planning/phases/04-announcements-events-resident-home/04-01-SUMMARY.md
  </read_first>
  <files>.planning/phases/04-announcements-events-resident-home/04-01-PLAN.md, .planning/phases/04-announcements-events-resident-home/04-RESEARCH.md</files>
  <action>Restore artifact traceability by replacing every stale Phase 4 planning reference to `supabase/migrations/0013_m08_announcements_events.sql` with `supabase/migrations/0019_m08_announcements_events.sql` in the original backend plan and its research notes. Update `files_modified`, must-have artifact paths, context references, acceptance criteria, and verify commands in `04-01-PLAN.md`; update the “Files likely to change” note in `04-RESEARCH.md`. Do not rewrite execution history in `04-01-SUMMARY.md`; this task is only for declared artifact traceability so the next verification pass can match plan intent to the implementation that actually exists.</action>
  <acceptance_criteria>
    - `.planning/phases/04-announcements-events-resident-home/04-01-PLAN.md` contains `0019_m08_announcements_events.sql`
    - `.planning/phases/04-announcements-events-resident-home/04-RESEARCH.md` contains `0019_m08_announcements_events.sql`
    - `rg -n "0013_m08_announcements_events\.sql" .planning/phases/04-announcements-events-resident-home/04-01-PLAN.md .planning/phases/04-announcements-events-resident-home/04-RESEARCH.md` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>rg -n "0019_m08_announcements_events\.sql|0013_m08_announcements_events\.sql" .planning/phases/04-announcements-events-resident-home/04-01-PLAN.md .planning/phases/04-announcements-events-resident-home/04-RESEARCH.md</automated>
  </verify>
  <done>The Phase 4 planning artifacts now point to the actual migration file that implements the backend contract, eliminating the documentation-only verification gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser resident → `event_attendees` | Resident RSVP payload is untrusted and must match the writable schema exactly |
| Planning docs → verification artifacts | Wrong artifact references can hide or misreport backend security contract coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | T | resident RSVP mutation payload | mitigate | Remove nonexistent `responded_at` and keep writes limited to `event_id`, `profile_id`, and `response` against RLS-protected rows |
| T-04-15 | R | SQL regression confidence | mitigate | Add real insert/update assertions to `m08_announcements_events_access.sql` so future contract drift fails in automation |
| T-04-16 | R | migration traceability | mitigate | Align Phase 4 plan/research artifact references to `0019_m08_announcements_events.sql` so verification evidence points at the real implemented contract |
</threat_model>

<verification>
Run typecheck and the targeted Phase 4 SQL regression, then grep the updated plan/research docs for the corrected migration reference.
</verification>

<success_criteria>
- Resident RSVP updates no longer fail because of a nonexistent column.
- Phase 4 SQL regression exercises a real RSVP write path.
- Plan/research documents reference the implemented `0019` migration, not the stale `0013` path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-announcements-events-resident-home/04-05-SUMMARY.md`
</output>
