---
phase: 02-billing-configuration-resident-billing-view
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - features/billing/ResidentInvoicesPage.tsx
  - features/billing/InvoiceDetailPage.tsx
  - lib/format.ts
autonomous: true
requirements:
  - BILL-06
  - BILL-07
must_haves:
  truths:
    - Resident sees invoice history from one billing area with filterable period/status views.
    - Resident can open invoice detail and see fee-item breakdown, due date, paid amount, and outstanding amount.
    - Resident can understand current dues, arrears context, and historical status labels without admin explanation.
  artifacts:
    - path: features/billing/ResidentInvoicesPage.tsx
      provides: Resident billing list with period/status filtering and summary totals.
      contains: Riwayat Tagihan
    - path: features/billing/InvoiceDetailPage.tsx
      provides: Resident invoice detail with itemized breakdown and payment state.
      contains: detail invoice
  key_links:
    - from: features/billing/ResidentInvoicesPage.tsx
      to: features/billing/InvoiceDetailPage.tsx
      via: invoice detail deep-link `/app/invoices/{id}`
      pattern: /app/invoices/
---

<objective>
Complete the resident-side billing experience so users can understand dues, arrears, and history from one trusted flow.

Purpose: Phase 2 is incomplete unless resident-facing billing clarity (not just admin configuration) is fully delivered.
Output: Updated resident invoice list + detail experience aligned to Indonesian neighborhood UX.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@features/billing/ResidentInvoicesPage.tsx
@features/billing/InvoiceDetailPage.tsx
@lib/format.ts
@lib/supabaseClient.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: refine resident invoice list for single billing experience clarity</name>
  <files>features/billing/ResidentInvoicesPage.tsx, lib/format.ts</files>
  <behavior>
    - Test 1: resident can filter list by status and period without breaking pagination.
    - Test 2: totals and badges reflect current filtered set and use consistent Indonesian labels.
    - Test 3: historical invoices remain visible read-only when resident has no active kavling mapping.
  </behavior>
  <action>Update `ResidentInvoicesPage.tsx` so BILL-07 is explicit: preserve one-screen clarity for current dues + historical status, keep warning copy for former residents, and ensure status labels/formatting come from shared `lib/format.ts` helpers (no duplicated inline label maps). Keep query scope dependent on existing RLS/history rules from Phase 1.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Resident billing list gives clear, filterable, and policy-aligned current/historical billing insight from one page.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: complete invoice detail breakdown for dues, items, and status context</name>
  <files>features/billing/InvoiceDetailPage.tsx</files>
  <behavior>
    - Test 1: invoice detail shows itemized fee rows and amount math (due/paid/outstanding) correctly.
    - Test 2: due-date and status context are clearly visible and consistent with list labels.
    - Test 3: invoice detail remains usable with submission-history and payment-form sections already in place.
  </behavior>
  <action>Refine `InvoiceDetailPage.tsx` to fully satisfy BILL-06 and BILL-07: keep item breakdown readable, ensure due-date + arrears-related context is explicit in copy/state badges, and maintain existing integration with payment submission/history components without regressing access guards.</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Resident invoice detail reliably explains what is owed, why, and what happened historically for each invoice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resident browser → invoice data API | Resident can attempt to query invoice records outside own scope. |
| UI status rendering → payment/billing decisions | Misleading labels or totals can cause incorrect resident actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I | Resident invoice list/detail queries | mitigate | Continue relying on invoice RLS/history helper scope; avoid client-side broad joins or bypass paths. |
| T-02-08 | T | Amount/status presentation | mitigate | Derive totals and labels from fetched invoice fields + shared formatter functions only, no manual hardcoded states. |
| T-02-09 | R | Resident billing interpretation | accept | UI copy clarity risk is low-impact security-wise; mitigate operationally with explicit Indonesian descriptions and consistent status badges. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Residents can understand invoice breakdown, dues, and history from one billing flow.
- Filters, labels, and totals remain consistent across list and detail screens.
- No regression to resident invoice-access boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-billing-configuration-resident-billing-view/02-03-SUMMARY.md`
</output>
