---
phase: 02-billing-configuration-resident-billing-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/sql/m08_phase2_billing_generation.sql
  - supabase/migrations/0013_m08_phase2_billing_rules.sql
  - package.json
autonomous: true
requirements:
  - BILL-02
  - BILL-05
must_haves:
  truths:
    - Admin can generate invoices for active kavlings from one selected billing period.
    - Generated invoices include recurring fee items with kavling override amounts applied when override windows are active.
    - Overdue invoices can receive penalty items from active penalty rules without duplicating the same rule on one invoice.
  artifacts:
    - path: supabase/migrations/0013_m08_phase2_billing_rules.sql
      provides: Invoice generation + penalty application SQL contracts for Phase 2.
      contains: create or replace function public.apply_penalties_for_period
    - path: supabase/tests/sql/m08_phase2_billing_generation.sql
      provides: SQL regression checks for generation, override resolution, and penalty behavior.
      contains: generate_invoices_for_period
  key_links:
    - from: supabase/migrations/0013_m08_phase2_billing_rules.sql
      to: supabase/tests/sql/m08_phase2_billing_generation.sql
      via: SQL function assertions for generation and penalties
      pattern: generate_invoices_for_period|apply_penalties_for_period|invoice_penalties
---

<objective>
Complete the backend billing-generation contract so invoice creation and penalty application are deterministic, auditable, and safe under RLS.

Purpose: Phase 2 cannot be trusted if invoice rows, override amounts, and overdue penalties are inconsistent across runs.
Output: New migration + SQL regression suite wired into default test:sql execution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/0002_tables.sql
@supabase/migrations/0010_m04_yearly_fee_cycle.sql
@supabase/migrations/0012_m07_access_scope_identity.sql
@supabase/tests/sql/m01_acceptance_checks.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write SQL contract tests for generation + penalties</name>
  <files>supabase/tests/sql/m08_phase2_billing_generation.sql</files>
  <behavior>
    - Test 1: `generate_invoices_for_period` creates invoices only for active kavlings and only once per kavling-period.
    - Test 2: generated invoice items resolve amount from `kavling_fee_overrides` when an override is active for that period.
    - Test 3: overdue invoices receive penalty rows from active `penalty_rules`, and rerun does not duplicate `(invoice_id, penalty_rule_id)`.
  </behavior>
  <action>Create `supabase/tests/sql/m08_phase2_billing_generation.sql` as the executable contract for BILL-02 and BILL-05. Seed minimal fixtures for one period, two kavlings, recurring fee types, one override window, and one active penalty rule. Assert first-run creation count, invoice-item amounts, and penalty insertion; assert idempotency on rerun. Keep test script compatible with `supabase db query --file ...` and the repo SQL test flow.</action>
  <verify>
    <automated>supabase db query --file supabase/tests/sql/m08_phase2_billing_generation.sql</automated>
  </verify>
  <done>Phase 2 billing-generation and penalty expectations are encoded in a repeatable SQL contract test.</done>
</task>

<task type="auto">
  <name>task 2: implement phase-2 SQL functions and wire test suite</name>
  <files>supabase/migrations/0013_m08_phase2_billing_rules.sql, package.json</files>
  <action>Create `0013_m08_phase2_billing_rules.sql` to finalize backend behavior required by BILL-02 and BILL-05: preserve existing yearly/monthly invoice generation behavior, add an explicit `apply_penalties_for_period(target_period_id uuid)` function (security definer, admin-like guard), and ensure penalty records are upsert-safe per invoice+rule while updating invoice totals/status consistently. Update `package.json` `test:sql` script so `m08_phase2_billing_generation.sql` runs in the standard SQL suite.</action>
  <verify>
    <automated>npm run test:sql</automated>
  </verify>
  <done>Backend invoice generation and penalty application pass automated SQL regression and are included in default verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → SQL RPC/functions | Privileged actions create financial obligations for many residents at once. |
| SQL function → invoice/payment tables | Incorrect writes can corrupt billing truth across app surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `generate_invoices_for_period` | mitigate | Keep uniqueness constraints and idempotent conflict handling so reruns cannot duplicate one kavling-period invoice. |
| T-02-02 | E | `apply_penalties_for_period` | mitigate | Require `is_admin_like()` check inside security-definer function before penalty writes. |
| T-02-03 | I | Override/penalty amount resolution | mitigate | Resolve only active-window overrides and active penalty rules; persist computed rows to `invoice_items`/`invoice_penalties` for auditability. |
</threat_model>

<verification>
- `supabase db query --file supabase/tests/sql/m08_phase2_billing_generation.sql`
- `npm run test:sql`
</verification>

<success_criteria>
- Invoice generation is deterministic per active kavling + period.
- Override windows affect generated amounts correctly.
- Overdue penalty application is available and idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/02-billing-configuration-resident-billing-view/02-01-SUMMARY.md`
</output>
