---
phase: 02-billing-configuration-resident-billing-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0013_m08_phase2_billing_rules.sql
  - supabase/tests/sql/m02_phase2_billing.sql
  - package.json
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-04
  - BILL-05
must_haves:
  truths:
    - Admin can preview invoice results for a billing period before creating new invoices.
    - Re-running invoice generation only creates missing invoices and never overwrites an existing kavling-period invoice.
    - Admin can preview and apply flat overdue penalties per cycle without duplicating the same invoice-cycle penalty.
    - Residents only see billing data when the billing period is published through the intended lifecycle state.
  artifacts:
    - path: supabase/migrations/0013_m08_phase2_billing_rules.sql
      provides: Preview, generation, penalty, and resident-visibility SQL contracts for Phase 2.
      contains: preview_invoices_for_period
    - path: supabase/tests/sql/m02_phase2_billing.sql
      provides: SQL regression coverage for preview, idempotent generation, publication visibility, and penalty cycles.
      contains: apply_penalties_for_period
    - path: package.json
      provides: Standard SQL suite wiring for the new Phase 2 contract test.
      contains: m02_phase2_billing.sql
  key_links:
    - from: supabase/migrations/0013_m08_phase2_billing_rules.sql
      to: supabase/tests/sql/m02_phase2_billing.sql
      via: SQL assertions covering preview, confirm, lifecycle, and penalty-cycle behavior
      pattern: preview_invoices_for_period|generate_invoices_for_period|apply_penalties_for_period
---

<objective>
Define the Phase 2 billing SQL contract so preview-before-confirm, idempotent generation, and repeatable penalty application become trustworthy and testable.

Purpose: Admin and resident UI cannot be planned safely until the database contract enforces D-01, D-02, D-03, D-07, D-08, D-09, D-10, and D-11.
Output: One migration, one phase-specific SQL regression file, and default SQL-suite wiring.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-billing-configuration-resident-billing-view/02-CONTEXT.md
@.planning/phases/02-billing-configuration-resident-billing-view/02-RESEARCH.md
@.planning/phases/02-billing-configuration-resident-billing-view/02-VALIDATION.md
@supabase/migrations/0002_tables.sql
@supabase/migrations/0005_rls_policies.sql
@supabase/migrations/0010_m04_yearly_fee_cycle.sql
@supabase/migrations/0012_m07_access_scope_identity.sql
@package.json

<interfaces>
From supabase/migrations/0010_m04_yearly_fee_cycle.sql:
```sql
create or replace function public.generate_invoices_for_period(target_period_id uuid)
returns integer
```

From supabase/migrations/0012_m07_access_scope_identity.sql:
```sql
create policy "billing_periods_admin_manage"
on public.billing_periods for all
to authenticated
using (public.has_finance_role())
with check (public.has_finance_role());
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write the SQL contract tests first</name>
  <files>supabase/tests/sql/m02_phase2_billing.sql</files>
  <read_first>
    - supabase/tests/sql/m01_acceptance_checks.sql
    - supabase/tests/sql/m01_generate_invoice_check.sql
    - supabase/migrations/0002_tables.sql
    - supabase/migrations/0010_m04_yearly_fee_cycle.sql
    - .planning/phases/02-billing-configuration-resident-billing-view/02-CONTEXT.md
    - .planning/phases/02-billing-configuration-resident-billing-view/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: preview returns kavling rows, fee-item rows, resolved override/default source, and period totals before any invoice insert (per D-01 and D-03).
    - Test 2: generation inserts only missing invoices for active kavlings and leaves existing kavling-period invoices untouched on rerun (per D-02).
    - Test 3: resident-facing visibility excludes draft periods and includes open/closed/archived periods according to lifecycle publication rules (per D-10, D-11, D-12).
    - Test 4: penalty preview/apply creates one flat-fee penalty item per invoice per cycle and rerun with the same cycle key does not duplicate rows (per D-07, D-08, D-09).
  </behavior>
  <action>Create `supabase/tests/sql/m02_phase2_billing.sql` as the executable acceptance contract for BILL-01, BILL-02, BILL-04, and BILL-05. Seed one draft billing period, one open period, at least two active kavlings, one inactive kavling, recurring fee types, one override window that covers the billing month, and one penalty fee setup. Assert that preview functions return kavling code, fee name, `default_amount`, `resolved_amount`, and `amount_source`; assert generation count only increases for missing invoices; assert draft-period resident reads remain hidden while open/closed/archived period rows remain readable; assert penalty application requires an explicit cycle key such as `2026-07` and stores only one penalty row/item for that invoice-cycle pair.</action>
  <acceptance_criteria>
    - `supabase/tests/sql/m02_phase2_billing.sql` contains `preview_invoices_for_period`
    - `supabase/tests/sql/m02_phase2_billing.sql` contains `apply_penalties_for_period`
    - `supabase/tests/sql/m02_phase2_billing.sql` contains `amount_source`
    - `supabase/tests/sql/m02_phase2_billing.sql` contains `2026-07`
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 must create supabase/tests/sql/m02_phase2_billing.sql first, then run `supabase db query --file supabase/tests/sql/m02_phase2_billing.sql`</automated>
  </verify>
  <done>Phase 2 SQL expectations are encoded before implementation and cover preview, confirm, lifecycle visibility, and penalty-cycle idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement preview, publish, and penalty-cycle SQL behavior</name>
  <files>supabase/migrations/0013_m08_phase2_billing_rules.sql, package.json</files>
  <read_first>
    - supabase/migrations/0002_tables.sql
    - supabase/migrations/0005_rls_policies.sql
    - supabase/migrations/0010_m04_yearly_fee_cycle.sql
    - supabase/migrations/0012_m07_access_scope_identity.sql
    - supabase/tests/sql/m02_phase2_billing.sql
    - package.json
  </read_first>
  <action>Create `supabase/migrations/0013_m08_phase2_billing_rules.sql` to extend the existing billing contract instead of replacing it. Add `preview_invoices_for_period(target_period_id uuid)` as a finance-role-guarded read function returning kavling code, fee metadata, `default_amount`, `resolved_amount`, `amount_source`, and period total per kavling; update `generate_invoices_for_period(target_period_id uuid)` so it keeps D-02 idempotency, resolves overrides per D-03, does not overwrite existing invoices, and does not auto-open the billing period; add penalty preview/apply functions that accept an explicit `cycle_key text`, create flat penalty invoice items using the configured penalty fee amount per D-07, and prevent duplicates for the same invoice-cycle pair per D-09. Update `package.json` so `npm run test:sql` includes `supabase/tests/sql/m02_phase2_billing.sql`.</action>
  <acceptance_criteria>
    - `supabase/migrations/0013_m08_phase2_billing_rules.sql` contains `create or replace function public.preview_invoices_for_period`
    - `supabase/migrations/0013_m08_phase2_billing_rules.sql` contains `cycle_key text`
    - `supabase/migrations/0013_m08_phase2_billing_rules.sql` does not contain `set status = 'open'`
    - `package.json` contains `m02_phase2_billing.sql`
  </acceptance_criteria>
  <verify>
    <automated>supabase db query --file supabase/tests/sql/m02_phase2_billing.sql</automated>
  </verify>
  <done>The Phase 2 migration enforces preview-before-confirm, publish gating, and repeatable penalty cycles, and the SQL suite exercises the contract end to end.</done>
</task>

<task type="auto">
  <name>task 3: [BLOCKING] push schema before verification</name>
  <files>supabase/migrations/0013_m08_phase2_billing_rules.sql</files>
  <read_first>
    - supabase/migrations/0013_m08_phase2_billing_rules.sql
    - package.json
  </read_first>
  <action>After all SQL file changes are complete, run `supabase db push` before any final verification command so the local database matches the migration state used by the TypeScript app and SQL tests. If the CLI requires auth or local stack startup, start the expected local Supabase environment first and rerun the push before claiming plan completion.</action>
  <acceptance_criteria>
    - `supabase db push` exits 0
    - `npm run test:sql` exits 0 after the push
  </acceptance_criteria>
  <verify>
    <automated>supabase db push && npm run test:sql</automated>
  </verify>
  <done>The live local schema reflects the migration before verification artifacts are produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finance-role client → SQL RPC/functions | Privileged billing actions can create or alter many resident obligations at once. |
| SQL functions → invoice and penalty tables | Incorrect writes can create duplicate or hidden financial state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `generate_invoices_for_period` | mitigate | Preserve unique `(billing_period_id, kavling_id)` handling and assert rerun idempotency in `m02_phase2_billing.sql`. |
| T-02-02 | E | preview/apply RPCs | mitigate | Require `public.has_finance_role()` or stricter checks inside every security-definer Phase 2 billing function. |
| T-02-03 | I | draft-period resident visibility | mitigate | Keep draft periods out of resident-readable policy/result paths and test lifecycle visibility explicitly. |
| T-02-04 | R | penalty-cycle application | mitigate | Persist explicit cycle-key-backed penalty records and test duplicate prevention for same invoice-cycle input. |
</threat_model>

<verification>
- `supabase db query --file supabase/tests/sql/m02_phase2_billing.sql`
- `supabase db push && npm run test:sql`
</verification>

<success_criteria>
- Admin can preview invoice effects before commit.
- Invoice generation stays additive-only for missing kavling-period invoices.
- Penalties apply as flat, auditable per-cycle additions without duplicates.
- Draft/open lifecycle controls resident visibility correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-billing-configuration-resident-billing-view/02-01-SUMMARY.md`
</output>
