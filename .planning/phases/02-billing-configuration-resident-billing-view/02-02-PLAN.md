---
phase: 02-billing-configuration-resident-billing-view
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - features/billing/BillingPeriodsPage.tsx
  - features/billing/BillingPeriodDetailPage.tsx
  - features/settings/FeeTypesPage.tsx
  - features/settings/FeeOverridesPage.tsx
autonomous: true
requirements:
  - BILL-01
  - BILL-03
  - BILL-04
must_haves:
  truths:
    - Admin can create billing periods with month/year/label/due-date and lifecycle state transitions.
    - Admin can manage recurring fee types (including yearly schedule fields) and configure default amounts.
    - Admin can manage per-kavling fee overrides with effective date windows and use them for generated invoices.
  artifacts:
    - path: features/billing/BillingPeriodsPage.tsx
      provides: Billing period CRUD + lifecycle + generate/apply penalty actions.
      contains: handleGenerateInvoices
    - path: features/settings/FeeTypesPage.tsx
      provides: Fee-type management for recurring/default/penalty settings.
      contains: Jenis Biaya
    - path: features/settings/FeeOverridesPage.tsx
      provides: Per-kavling override CRUD with active window controls.
      contains: kavling_fee_overrides
  key_links:
    - from: features/billing/BillingPeriodsPage.tsx
      to: supabase RPC/functions
      via: `generate_invoices_for_period` and penalty-application trigger path
      pattern: rpc\("generate_invoices_for_period"
---

<objective>
Finalize the admin billing surfaces so operators can configure period/rules and execute invoice operations without spreadsheet fallback.

Purpose: BILL-01/BILL-03/BILL-04 are delivered only when admin UI and backend contract are wired together safely.
Output: Updated billing/settings pages with auditable mutation flows.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@features/billing/BillingPeriodsPage.tsx
@features/billing/BillingPeriodDetailPage.tsx
@features/settings/FeeTypesPage.tsx
@features/settings/FeeOverridesPage.tsx
@lib/validation.ts
@supabase/migrations/0013_m08_phase2_billing_rules.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: tighten admin billing period lifecycle + generation actions</name>
  <files>features/billing/BillingPeriodsPage.tsx, features/billing/BillingPeriodDetailPage.tsx</files>
  <behavior>
    - Test 1: period create form enforces month/year/due-date/label validation and saves as draft first.
    - Test 2: admin can generate invoices for draft/open period and sees updated invoice counts.
    - Test 3: lifecycle state changes (`draft/open/closed/archived`) are role-guarded and auditable.
  </behavior>
  <action>Refine `BillingPeriodsPage.tsx` and `BillingPeriodDetailPage.tsx` to fully satisfy BILL-01 and operationally expose BILL-02/BILL-05 outputs from Plan 01: keep Indonesian copy, ensure validation errors are surfaced inline, keep generate action idempotent-safe in UI messaging, and add/keep explicit audit-log writes for create/state/generate actions. If adding penalty-application action in this UI, wire it to the Plan 01 SQL function and guard it to admin-like users only.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Admin billing-period lifecycle and generation actions are complete, validated, and auditable in UI.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: complete fee-type and kavling-override management flows</name>
  <files>features/settings/FeeTypesPage.tsx, features/settings/FeeOverridesPage.tsx</files>
  <behavior>
    - Test 1: fee-type form enforces recurring/yearly constraints for `billing_cycle` and `charge_month`.
    - Test 2: override form enforces active date window correctness and stores per-kavling amount overrides.
    - Test 3: create/update/activate/deactivate actions write audit logs with before/after data.
  </behavior>
  <action>Update `FeeTypesPage.tsx` and `FeeOverridesPage.tsx` to close BILL-03 and BILL-04 end-to-end: keep recurring vs penalty distinctions explicit, ensure yearly-charge month behavior maps to schema constraints, keep override list/filter usability for many rows, and preserve audit logging for every mutation path (`fee_type.*`, `fee_override.*`). Do not introduce new external dependencies or alternate state libraries.</action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Admin can maintain fee defaults and per-kavling overrides safely with validation + auditability intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI forms → Supabase writes | Untrusted browser payloads attempt financial-config mutations. |
| admin mutations → audit trail | Missing logs reduce non-repudiation for billing changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | Fee type / override forms | mitigate | Enforce zod validation for cycle/date/amount rules before writes and keep DB constraints as final guard. |
| T-02-05 | R | Admin billing mutations | mitigate | Keep `writeAuditLog` for create/update/status/generate operations with actor and before/after payloads. |
| T-02-06 | E | Billing lifecycle controls | mitigate | Keep role checks for reopening/privileged transitions and rely on RLS/admin-like guards server-side. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Billing period lifecycle and generation controls are complete for admins.
- Fee-type and override configuration supports launch billing rules.
- Every admin mutation path remains auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-billing-configuration-resident-billing-view/02-02-SUMMARY.md`
</output>
