---
phase: 02-billing-configuration-resident-billing-view
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - features/billing/BillingPeriodsPage.tsx
  - features/billing/BillingPeriodDetailPage.tsx
  - features/settings/FeeTypesPage.tsx
  - features/settings/FeeTypeForm.tsx
  - features/settings/FeeOverridesPage.tsx
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04
  - BILL-05
must_haves:
  truths:
    - Admin can create billing periods with lifecycle states and manage them with explicit publish/correction controls.
    - Admin can preview invoice generation and penalty application before confirming them.
    - Admin can manage fee defaults, penalty fee types, and per-kavling overrides with auditable writes.
  artifacts:
    - path: features/billing/BillingPeriodsPage.tsx
      provides: Admin billing period list with preview and lifecycle actions.
      contains: Pratinjau Tagihan
    - path: features/billing/BillingPeriodDetailPage.tsx
      provides: Admin period detail and invoice monitoring surface.
      contains: Detail Periode Billing
    - path: features/settings/FeeTypeForm.tsx
      provides: Fee-type form constraints for recurring, yearly, and penalty configuration.
      contains: Siklus tagihan
    - path: features/settings/FeeOverridesPage.tsx
      provides: Override management with date-window controls.
      contains: Override
  key_links:
    - from: features/billing/BillingPeriodsPage.tsx
      to: supabase/migrations/0013_m08_phase2_billing_rules.sql
      via: `preview_invoices_for_period`, `generate_invoices_for_period`, and penalty preview/apply RPC calls
      pattern: preview_invoices_for_period|generate_invoices_for_period|apply_penalties_for_period
---

<objective>
Build the admin billing operations UI so treasurers/admins can configure periods and rules, preview outcomes, and publish billing safely without spreadsheet fallback.

Purpose: BILL-01 through BILL-05 need an operator surface that directly reflects the SQL contract from Plan 01 and the approved UI design contract.
Output: Updated admin billing period, period detail, fee type, and override screens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-billing-configuration-resident-billing-view/02-CONTEXT.md
@.planning/phases/02-billing-configuration-resident-billing-view/02-RESEARCH.md
@.planning/phases/02-billing-configuration-resident-billing-view/02-UI-SPEC.md
@features/billing/BillingPeriodsPage.tsx
@features/billing/BillingPeriodDetailPage.tsx
@features/settings/FeeTypesPage.tsx
@features/settings/FeeTypeForm.tsx
@features/settings/FeeOverridesPage.tsx
@features/audit/writeAuditLog.ts
@lib/validation.ts
@supabase/migrations/0013_m08_phase2_billing_rules.sql

<interfaces>
From features/audit/writeAuditLog.ts:
```ts
export async function writeAuditLog(payload: AuditLogInput)
```

From lib/validation.ts:
```ts
export const billingPeriodFormSchema = z.object({ year, month, due_date, label })
export const feeTypeFormSchema = z.object({ code, name, default_amount, is_recurring, billing_cycle, charge_month, is_penalty, active, sort_order })
export const feeOverrideFormSchema = z.object({ kavling_id, fee_type_id, amount, active_from, active_until, notes })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: wire billing period preview, publish, and penalty actions into admin UI</name>
  <files>features/billing/BillingPeriodsPage.tsx, features/billing/BillingPeriodDetailPage.tsx</files>
  <read_first>
    - features/billing/BillingPeriodsPage.tsx
    - features/billing/BillingPeriodDetailPage.tsx
    - features/audit/writeAuditLog.ts
    - lib/validation.ts
    - .planning/phases/02-billing-configuration-resident-billing-view/02-CONTEXT.md
    - .planning/phases/02-billing-configuration-resident-billing-view/02-UI-SPEC.md
    - supabase/migrations/0013_m08_phase2_billing_rules.sql
  </read_first>
  <behavior>
    - Test 1: period creation stays draft-first and only open periods become published to residents (per D-10 and D-11).
    - Test 2: the primary invoice-generation CTA opens a preview-first flow and uses fully Indonesian copy `Pratinjau Tagihan` before `Buat Tagihan` confirmation (UI flag).
    - Test 3: penalty application follows the same preview-then-confirm pattern and shows the cycle key before confirmation (per D-08 and UI spec).
  </behavior>
  <action>Update `BillingPeriodsPage.tsx` and `BillingPeriodDetailPage.tsx` to consume the Plan 01 RPC contract. Replace any direct one-click generate action with a preview-first flow that calls `preview_invoices_for_period`, renders kavling, fee item, `amount_source`, and period total, then confirms with `generate_invoices_for_period`. Use fully Indonesian CTA labels: row/list entry point `Pratinjau Tagihan`, confirm action `Buat Tagihan`, penalty preview action `Pratinjau Denda`, and penalty confirm action `Terapkan Denda`. Keep lifecycle controls explicit and labeled (`Buka Periode`, `Tutup Periode`, `Arsipkan Periode`, `Buka Ulang Periode`), preserve reopen restriction to admin/super_admin, and record audit entries for create, status change, generation, and penalty application. Match the UI contract by styling draft as unpublished/editable, open as published, archived as read-only, and by avoiding icon-only controls for sensitive actions.</action>
  <acceptance_criteria>
    - `features/billing/BillingPeriodsPage.tsx` contains `Pratinjau Tagihan`
    - `features/billing/BillingPeriodsPage.tsx` contains `Pratinjau Denda`
    - `features/billing/BillingPeriodsPage.tsx` contains `preview_invoices_for_period`
    - `features/billing/BillingPeriodsPage.tsx` contains `writeAuditLog`
    - `features/billing/BillingPeriodDetailPage.tsx` contains `Buka Periode`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Admin period management reflects preview-before-confirm billing operations, explicit lifecycle controls, and auditable actions.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: finish fee and override management for recurring, yearly, and penalty billing rules</name>
  <files>features/settings/FeeTypesPage.tsx, features/settings/FeeTypeForm.tsx, features/settings/FeeOverridesPage.tsx</files>
  <read_first>
    - features/settings/FeeTypesPage.tsx
    - features/settings/FeeTypeForm.tsx
    - features/settings/FeeOverridesPage.tsx
    - features/audit/writeAuditLog.ts
    - lib/validation.ts
    - .planning/phases/02-billing-configuration-resident-billing-view/02-CONTEXT.md
    - .planning/phases/02-billing-configuration-resident-billing-view/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: fee-type forms enforce monthly/yearly constraints and allow flat penalty fee configuration per D-07.
    - Test 2: override rows remain date-window-based and clearly distinguish active versus expired settings per D-03.
    - Test 3: every create/update/activate/deactivate path writes audit logs and keeps Indonesian operator copy.
  </behavior>
  <action>Refine `FeeTypesPage.tsx`, `FeeTypeForm.tsx`, and `FeeOverridesPage.tsx` so BILL-03 and BILL-04 are fully operator-ready. Keep `billing_cycle`, `charge_month`, `is_penalty`, and `default_amount` aligned with `feeTypeFormSchema`; expose flat penalty fee setup in Indonesian terminology (`denda flat per periode terlambat`) rather than percentage language; keep override date-window editing visible and sortable for many rows; and preserve audit logging for `fee_type.create`, `fee_type.update`, `fee_type.activate`, `fee_type.deactivate`, and the equivalent override actions. Do not add a new dependency or global state library.</action>
  <acceptance_criteria>
    - `features/settings/FeeTypeForm.tsx` contains `denda`
    - `features/settings/FeeTypeForm.tsx` contains `billingCycle`
    - `features/settings/FeeOverridesPage.tsx` contains `active_until`
    - `features/settings/FeeTypesPage.tsx` contains `writeAuditLog`
    - `features/settings/FeeOverridesPage.tsx` contains `writeAuditLog`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Finance operators can maintain recurring fees, penalty fees, and per-kavling overrides with validation and auditability intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finance-role browser forms → Supabase writes | Untrusted browser payloads can change neighborhood billing rules. |
| admin UI actions → audit log RPC | Missing audit writes would weaken finance traceability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | fee and override forms | mitigate | Keep zod validation in UI and DB constraints in SQL; do not bypass typed form schemas. |
| T-02-06 | R | billing lifecycle and generation actions | mitigate | Record every privileged mutation via `writeAuditLog` with before/after payloads and explicit action names. |
| T-02-07 | E | reopen/archive/penalty controls | mitigate | Keep role-specific guards in UI and rely on finance-role checks in RPCs for final enforcement. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Admin can preview and confirm invoice generation and penalty application from the billing screens.
- Billing period lifecycle controls are explicit, auditable, and aligned to draft/open/closed/archived semantics.
- Fee-type and override management fully support launch billing rules.
</success_criteria>

<output>
After completion, create `.planning/phases/02-billing-configuration-resident-billing-view/02-02-SUMMARY.md`
</output>
