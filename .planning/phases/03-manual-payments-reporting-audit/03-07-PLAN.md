---
phase: 03-manual-payments-reporting-audit
plan: 07
type: execute
wave: 4
depends_on:
  - 03-06
files_modified:
  - features/reports/ReportsPage.tsx
autonomous: true
requirements:
  - RPRT-01
  - RPRT-02
  - RPRT-03
  - RPRT-04
  - PAY-07
gap_closure: true
must_haves:
  truths:
    - `/admin/reports` still loads collection summary and arrears data even when receipt-output or candidate helpers fail.
    - Operators generate resident receipts from one payment candidate at a time, and the new artifact appears in output history immediately after success.
    - CSV export remains tied to the same summary and arrears data shown on screen per D-09.
  artifacts:
    - path: features/reports/ReportsPage.tsx
      provides: Resilient reports UI with partial-failure handling, manual refresh, and payment-specific receipt generation.
      contains: Terakhir diperbarui
  key_links:
    - from: features/reports/ReportsPage.tsx
      to: features/reports/reportQueries.ts
      via: summary, arrears, outputs, and receipt-candidate loads
      pattern: loadCollectionSummary|loadArrearsList|loadGeneratedReportOutputs|loadReceiptCandidates
    - from: features/reports/ReportsPage.tsx
      to: features/reports/reportOutputClient.ts
      via: payment-specific receipt generation and signed-download actions
      pattern: generateReportOutputArtifact|getReportOutputSignedUrl
---

<objective>
Restore `/admin/reports` as a trustworthy operator surface by isolating receipt-helper failures, removing placeholder receipt generation, and wiring receipt actions to the repaired payment-specific artifact contract.

Purpose: `03-VERIFICATION.md` shows the reports page can currently fail before summary/arrears/CSV are usable, and its top-level receipt action still uses a zero-UUID placeholder instead of a real payment candidate; this plan fixes the operator workflow without changing established resident UX.
Output: A resilient reports page that keeps core reporting alive during partial failures and generates real resident receipts from payment candidates only.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manual-payments-reporting-audit/03-CONTEXT.md
@.planning/phases/03-manual-payments-reporting-audit/03-VERIFICATION.md
@.planning/phases/03-manual-payments-reporting-audit/03-06-PLAN.md
@.planning/phases/03-manual-payments-reporting-audit/03-UI-SPEC.md
@features/reports/ReportsPage.tsx
@features/reports/reportQueries.ts
@features/reports/reportOutputClient.ts

<interfaces>
From features/reports/reportQueries.ts:
```ts
export async function loadCollectionSummary(billingPeriodId: string): Promise<CollectionSummaryRow[]>;
export async function loadArrearsList(billingPeriodId: string): Promise<ArrearsRow[]>;
export async function loadGeneratedReportOutputs(billingPeriodId: string): Promise<GeneratedReportOutputRow[]>;
export async function loadReceiptCandidates(billingPeriodId: string): Promise<ReceiptCandidateRow[]>;

export interface ReceiptCandidateRow {
  invoice_id: string;
  invoice_number: string;
  kavling_id: string;
  kavling_code: string;
  payment_id: string;
  amount_paid: number;
  payment_date: string;
}
```

From features/reports/reportOutputClient.ts:
```ts
export type GenerateReportOutputPayload =
  | {
      reportType: "monthly_summary";
      billingPeriodId: string;
      title: string;
      metadata: MonthlySummaryMetadata;
    }
  | {
      reportType: "receipt";
      billingPeriodId: string;
      invoiceId: string;
      paymentId?: string;
      title: string;
      metadata: ResidentReceiptMetadata;
    };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: isolate receipt-helper failures so core reporting stays available per D-12 and D-13</name>
  <files>features/reports/ReportsPage.tsx</files>
  <read_first>
    - features/reports/ReportsPage.tsx
    - features/reports/reportQueries.ts
    - .planning/phases/03-manual-payments-reporting-audit/03-VERIFICATION.md
    - .planning/phases/03-manual-payments-reporting-audit/03-CONTEXT.md
    - .planning/phases/03-manual-payments-reporting-audit/03-UI-SPEC.md
  </read_first>
  <action>Refactor `loadReportData()` in `features/reports/ReportsPage.tsx` so `loadCollectionSummary(selectedPeriodId)` and `loadArrearsList(selectedPeriodId)` remain the primary truth for RPRT-01/RPRT-02/RPRT-03, while `loadGeneratedReportOutputs(selectedPeriodId)` and `loadReceiptCandidates(selectedPeriodId)` are loaded independently and cannot blank the summary/arrears tables. Replace the current single `Promise.all([...])` failure path with separate awaits or `Promise.allSettled`, keep `summaryRows`/`arrearsRows` populated when output or candidate loads fail, and set the Indonesian reconciliation warning text through `outputError` plus the existing refresh CTA per D-12/D-13. Update `lastRefreshed` only when the primary report data succeeds so the freshness indicator remains trustworthy.</action>
  <acceptance_criteria>
    - `features/reports/ReportsPage.tsx` no longer uses a single `Promise.all([ loadCollectionSummary, loadArrearsList, loadGeneratedReportOutputs, loadReceiptCandidates ])` expression.
    - `features/reports/ReportsPage.tsx` keeps separate handling for summary/arrears versus output/candidate failures.
    - `features/reports/ReportsPage.tsx` contains Indonesian partial-failure/reconciliation copy plus the existing refresh CTA.
    - `features/reports/ReportsPage.tsx` still contains `Terakhir diperbarui`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>The reports page continues to serve collection summary, arrears, and CSV workflows even when receipt-output helpers have a temporary fault, and operators see an explicit reconciliation warning instead of a blank page.</done>
</task>

<task type="auto">
  <name>task 2: remove placeholder receipt generation and switch operator actions to payment-specific artifacts</name>
  <files>features/reports/ReportsPage.tsx</files>
  <read_first>
    - features/reports/ReportsPage.tsx
    - features/reports/reportQueries.ts
    - features/reports/reportOutputClient.ts
    - .planning/phases/03-manual-payments-reporting-audit/03-VERIFICATION.md
  </read_first>
  <action>Delete the period-wide `handleGenerateReceiptReport()` flow and remove the top-level `Buat Laporan Bukti Bayar` button that currently sends the placeholder invoice id `00000000-0000-0000-0000-000000000000`. Keep the monthly summary button. In `handleGenerateResidentReceipt(candidate)`, pass the real `paymentId: candidate.payment_id` together with `invoiceId: candidate.invoice_id`, then reload report outputs/candidates after success so the new artifact row appears immediately in `Output Laporan yang Dibuat`. Preserve D-09 by leaving CSV export bound only to `summaryRows` / `arrearsRows`, not receipt candidates. If the post-generation refresh of outputs/candidates fails, keep the success state for the generated artifact request but surface the D-13 reconciliation warning with manual refresh rather than wiping already loaded reporting data.</action>
  <acceptance_criteria>
    - `features/reports/ReportsPage.tsx` does not contain `00000000-0000-0000-0000-000000000000`.
    - `features/reports/ReportsPage.tsx` does not render the top-level `Buat Laporan Bukti Bayar` button anymore.
    - `features/reports/ReportsPage.tsx` passes `paymentId: candidate.payment_id` when generating resident receipts.
    - `features/reports/ReportsPage.tsx` refreshes output history after successful receipt generation.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Receipt generation is payment-specific, placeholder artifact creation is gone, and operators immediately see/download the artifact generated from the selected candidate row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reports UI → reporting query helpers | Partial helper failure must not collapse unrelated finance data or hide divergence from the operator. |
| reports UI → report-output generation | Operator-selected receipt actions must target only real payment candidates, not placeholder identifiers. |
| reports UI → CSV/export actions | CSV output must remain tied to the same summary/arrears data shown on screen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-28 | T | `/admin/reports` receipt generation action | mitigate | Remove the placeholder zero-UUID flow and require `candidate.payment_id` for every receipt generation request. |
| T-03-29 | R | report freshness / divergence handling | mitigate | Keep `Terakhir diperbarui`, preserve loaded summary data, and surface explicit reconciliation warning + refresh CTA on partial failure per D-12/D-13. |
| T-03-30 | D | reports page availability | mitigate | Split primary summary/arrears loads from secondary output/candidate loads so one helper fault cannot take down the whole screen. |
| T-03-31 | I | CSV/reporting truth alignment | mitigate | Continue driving CSV export from `summaryRows` and `arrearsRows`, the same data structures rendered on screen per D-09. |
</threat_model>

<verification>
- `npm run typecheck && npm run build`
- Confirm `features/reports/ReportsPage.tsx` no longer contains the placeholder receipt UUID.
- Confirm payment-specific receipt generation refreshes output history immediately after success.
</verification>

<success_criteria>
1. `/admin/reports` remains usable for summary, arrears, and CSV even if receipt-output helpers temporarily fail.
2. Receipt generation is driven by real `payment_id` candidates only, with no placeholder invoice flow left in the code.
3. Output history and reconciliation messaging keep operators aligned to the same live finance truth shown on screen.
</success_criteria>

<output>
After completion, create `.planning/phases/03-manual-payments-reporting-audit/03-07-SUMMARY.md`
</output>
