---
phase: 03-manual-payments-reporting-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0016_m09_manual_payment_reporting_contract.sql
  - supabase/tests/sql/m08_phase3_payment_reporting.sql
  - package.json
autonomous: true
requirements:
  - PAY-04
  - PAY-07
  - RPRT-05
must_haves:
  truths:
    - Treasurer/admin verification and rejection always produce auditable, consistent invoice/payment state transitions.
    - Re-verify/re-reject/double-apply paths are blocked and return deterministic errors.
    - Finance audit evidence includes enough before/after data to explain every sensitive payment decision.
  artifacts:
    - path: supabase/migrations/0016_m09_manual_payment_reporting_contract.sql
      provides: Hardened RPC and audit contract for payment verification/rejection consistency.
      contains: create or replace function public.verify_payment_submission
    - path: supabase/tests/sql/m08_phase3_payment_reporting.sql
      provides: SQL regression coverage for verification/audit/reporting invariants.
      contains: phase3 payment reporting checks
    - path: package.json
      provides: SQL suite wiring for M08 acceptance checks.
      contains: m08_phase3_payment_reporting.sql
  key_links:
    - from: supabase/migrations/0013_m08_manual_payment_reporting_contract.sql
      to: public.payment_submissions
      via: verify/reject RPC transaction logic
      pattern: verify_payment_submission|reject_payment_submission
    - from: supabase/migrations/0013_m08_manual_payment_reporting_contract.sql
      to: public.audit_logs
      via: audit insert per decision path
      pattern: insert into public.audit_logs
---

<objective>
Stabilize Phase 3 finance-state truth at the database layer so verification/rejection outcomes are deterministic, auditable, and safe for downstream reporting.

Purpose: Reporting and resident/admin screens only stay trustworthy when invoice/payment transitions are atomically correct and regression-guarded.
Output: One migration + one SQL acceptance suite that codifies verification/rejection/audit invariants.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/0011_m06_verification_audit.sql
@supabase/migrations/0012_m07_access_scope_identity.sql
@supabase/tests/sql/m07_phase1_access_identity.sql

<interfaces>
From supabase/migrations/0011_m06_verification_audit.sql:
```sql
create or replace function public.recalculate_invoice_status(target_invoice_id uuid)
returns public.invoice_status;

create or replace function public.verify_payment_submission(target_submission_id uuid, admin_note text default null)
returns uuid;

create or replace function public.reject_payment_submission(target_submission_id uuid, reason text)
returns void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write SQL regression contract for payment-state and audit invariants</name>
  <files>supabase/tests/sql/m08_phase3_payment_reporting.sql</files>
  <behavior>
    - Test 1: verifying a submitted payment creates one payment row, updates invoice status/amount_paid, and appends audit log evidence.
    - Test 2: rejecting a submitted payment writes rejection reason, recalculates invoice status, and appends rejection audit log evidence.
    - Test 3: non-submitted submissions cannot be re-verified/re-rejected and must fail deterministically.
  </behavior>
  <action>Create `m08_phase3_payment_reporting.sql` first (RED) to lock PAY-04/PAY-07/RPRT-05 invariants. Include checks for actor role scope (`treasurer` + `admin` allowed), duplicate transition rejection, and before/after audit payload completeness. Reuse existing SQL test style and transaction wrappers from prior `m0x_*.sql` files.</action>
  <verify>
    <automated>npm run test:sql</automated>
  </verify>
  <done>SQL checks fail before migration changes and encode all required finance-state/audit invariants for Phase 3.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement migration changes that satisfy the new regression contract</name>
  <files>supabase/migrations/0013_m08_manual_payment_reporting_contract.sql, package.json</files>
  <behavior>
    - Test 1: verification/rejection functions satisfy all new SQL assertions without changing Phase 1 scope guarantees.
    - Test 2: audit log entries include deterministic action, entity linkage, and after-data payload needed by finance operations.
    - Test 3: invoice status recalculation remains idempotent and consistent after repeated reads.
  </behavior>
  <action>Add `0013_m08_manual_payment_reporting_contract.sql` to harden finance RPC behavior (transaction-safe checks, deterministic errors, explicit guardrails for invalid state transitions) and wire the M08 SQL file into `package.json` `test:sql`. Keep implementation in SQL/RPC layer (not client-side workaround) to preserve RLS-first architecture.</action>
  <verify>
    <automated>npm run supabase:reset -- --yes && npm run test:sql</automated>
  </verify>
  <done>All M08 checks pass, prior SQL suites remain green, and payment-state truth is auditable and stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| treasurer/admin client → security-definer RPC | Privileged invoice/payment transitions are triggered from untrusted client input. |
| RPC transaction logic → audit ledger | Missing/incorrect audit writes can break non-repudiation for finance actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | E | `verify_payment_submission` / `reject_payment_submission` | mitigate | Enforce role guard with existing helper checks and reject unauthorized callers at function entry. |
| T-03-02 | T | Invoice/payment status transitions | mitigate | Reject non-submitted transition paths and assert one-way transition semantics in SQL checks. |
| T-03-03 | R | `public.audit_logs` evidence for finance decisions | mitigate | Persist before/after payload plus actor/action metadata on every verify/reject path. |
| T-03-04 | D | Repeated verification calls on same submission | mitigate | Add deterministic state checks and return explicit error before side effects. |
</threat_model>

<verification>
- `npm run test:sql`
- `npm run supabase:reset -- --yes && npm run test:sql`
</verification>

<success_criteria>
- Verify/reject workflows are transaction-safe and deterministic at SQL layer.
- Audit logs can explain every sensitive verification decision.
- Regressions in payment-state truth fail automatically in SQL acceptance checks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-manual-payments-reporting-audit/03-01-SUMMARY.md`
</output>
